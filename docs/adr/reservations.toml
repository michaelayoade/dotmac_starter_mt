# The Starter ADR number register — the allocator, not a summary of one.
#
# Starter allocated ADR numbers by reading `docs/adr/` and taking the next free
# one. That is a read-then-write with no lock, performed on branches that
# cannot see each other. On 2026-09-04 it produced the race it looks like:
# THREE decisions were authored against 0074 within eighty minutes — the Tax
# ownership record (`0f8b0746`, 09:39:14) and the two Foundation records
# (`92f3b977`, 09:40:58) — by two authors who each did the correct thing under
# the old rule and neither of whom could see the other. This file replaces that
# rule with a serialized allocator.
#
# Ported from `dotmac_erp` at `5b2a035f` under ADR-0006's product-first
# extraction rule; the dossier is `docs/adr/EXTRACTION.toml`. It stays
# repository-local because each repository owns its own ADR namespace, and the
# 0001-0009 band here shares its spelling with ERP's own decisions.
#
# HOW TO CLAIM A NUMBER
#
#   1. Take `next_free`. That is your number. Do NOT look at `docs/adr/` and
#      take the next gap — see WHY THE DIRECTORY IS NOT THE ALLOCATOR below.
#   2. Append a `[[reservation]]` row with `status = "reserved"`, and raise
#      `next_free` by one.
#   3. Commit THAT ALONE, and land it on `main` before you write the ADR.
#   4. Cut the authoring branch from a `main` that already carries your row.
#      Write the ADR and flip your row to `status = "authored"` in that change.
#
# WHY IT SERIALIZES
#
# `next_free` is one scalar on one line. Two branches that both allocate must
# both rewrite it, so git cannot silently auto-merge them — it raises a
# conflict, and a conflict is the lock being contended rather than a duplicate
# being created. Appended rows alone would not be enough: git will often merge
# two appends at the end of a file without complaint, and that is precisely how
# 0074 came to name three decisions.
#
# WHY THE DIRECTORY IS NOT THE ALLOCATOR
#
# `docs/adr/` on a feature branch shows what has merged plus what that branch
# wrote. It cannot show what a sibling branch wrote, and it cannot show what
# landed on `main` after the branch was cut. Every one of the three 0074
# authors read the directory, found 0073 the highest, took 0074, and was right
# about everything they could see. A procedure that is correct from every
# vantage point and still produces a collision is not a procedure anyone can
# follow more carefully; it is the wrong procedure. The register is the only
# allocator, and step 3 is what makes a claim visible to the next author.
#
# Step 3 is also the load-bearing one for the gate. A number claimed in the
# same change that authors the ADR was claimed on a branch, and a claim on a
# branch is not a claim — `unreserved_authorings` in the checker refuses an ADR
# whose number was not already reserved at the branch's MERGE BASE.
#
# PERMANENCE
#
# A number, once in this file, is never removed, reused, or reassigned to a
# different decision. An abandoned ADR takes `status = "withdrawn"` and keeps
# its number forever. Reuse is not a tidy-up: the number appears in commit
# messages, review threads, other repositories' pins, `docs/inventories/`
# snapshots and cross-repository ADR citations, and none of those are updated
# by deleting a row. A withdrawn record may be re-authored under a NEW number,
# declaring `replaces = <old number>` so the trail survives.
#
# STATUS VALUES
#
#   reserved   number claimed, no document on `main`. A branch that authors it
#              flips the row to `authored` in the same change.
#   authored   `NNNN-<slug>.md` exists in this directory.
#   withdrawn  abandoned. The number is spent forever. No file may exist.
#   contested  PRE-EXISTING DEBT ONLY. More than one unmerged decision claims
#              this number and none is on `main`. Empty today, and the checker
#              refuses a new one — the register exists to prevent one.

next_free = 77

# ---------------------------------------------------------------------------
# Authored — on `main`. `claimed` is the date the file first landed there.
# ---------------------------------------------------------------------------

[[reservation]]
number = 1
slug = "multi-tenant-architecture"
status = "authored"
claimed = "2026-05-04"

[[reservation]]
number = 2
slug = "starter-consolidation"
status = "authored"
claimed = "2026-07-17"

[[reservation]]
number = 3
slug = "unified-deployment-profiles"
status = "authored"
claimed = "2026-07-19"

[[reservation]]
number = 4
slug = "platform-control-plane"
status = "authored"
claimed = "2026-07-30"

[[reservation]]
number = 5
slug = "forgejo-private-registry"
status = "authored"
claimed = "2026-07-30"

[[reservation]]
number = 6
slug = "white-label-product-foundation"
status = "authored"
claimed = "2026-08-06"

[[reservation]]
number = 7
slug = "deployment-authenticated-applied-state"
status = "authored"
claimed = "2026-08-04"

[[reservation]]
number = 8
slug = "manifest-declaration-registries"
status = "authored"
claimed = "2026-08-08"

[[reservation]]
number = 9
slug = "secrets-are-held-not-dereferenced"
status = "authored"
claimed = "2026-08-08"

[[reservation]]
number = 10
slug = "adapters-are-thin"
status = "authored"
claimed = "2026-08-08"

[[reservation]]
number = 11
slug = "settings-resolution-reads-rows-and-defaults"
status = "authored"
claimed = "2026-08-08"

[[reservation]]
number = 12
slug = "a-setting-declares-whether-it-inherits"
status = "authored"
claimed = "2026-08-09"

[[reservation]]
number = 13
slug = "the-platform-declares-deployment-facts"
status = "authored"
claimed = "2026-08-09"

[[reservation]]
number = 14
slug = "at-most-once-execution-has-one-owner"
status = "authored"
claimed = "2026-08-10"

[[reservation]]
number = 15
slug = "academy-is-an-assembly-or-it-is-not"
status = "authored"
claimed = "2026-08-11"

[[reservation]]
number = 16
slug = "payment-coverage-is-derived-not-a-status"
status = "authored"
claimed = "2026-08-11"

[[reservation]]
number = 17
slug = "adoption-is-the-scarce-resource"
status = "authored"
claimed = "2026-08-11"

[[reservation]]
number = 18
slug = "an-exemption-must-be-enforceable"
status = "authored"
claimed = "2026-08-11"

[[reservation]]
number = 19
slug = "party-identity-follows-the-archetype"
status = "authored"
claimed = "2026-08-12"

[[reservation]]
number = 20
slug = "billing-owns-operational-receivables"
status = "authored"
claimed = "2026-08-12"

[[reservation]]
number = 21
slug = "the-tenant-workspace-is-a-third-plane"
status = "authored"
claimed = "2026-08-12"

[[reservation]]
number = 22
slug = "files-own-stored-bytes-not-domain-meaning"
status = "authored"
claimed = "2026-08-13"

[[reservation]]
number = 23
slug = "dual-plane-modules-declare-both-persistence-planes"
status = "authored"
claimed = "2026-08-13"

[[reservation]]
number = 24
slug = "apps-compose-by-synchronizing-data"
status = "authored"
claimed = "2026-08-13"

[[reservation]]
number = 25
slug = "imports-own-the-run-not-what-a-row-means"
status = "authored"
claimed = "2026-08-13"

[[reservation]]
number = 26
slug = "approvals-decide-approval-never-the-transition"
status = "authored"
claimed = "2026-08-14"

[[reservation]]
number = 27
slug = "a-plane-declares-its-own-prerequisites"
status = "authored"
claimed = "2026-08-14"

[[reservation]]
number = 28
slug = "an-assembly-declares-each-module-plane"
status = "authored"
claimed = "2026-08-14"

[[reservation]]
number = 29
slug = "access-is-requested-approved-and-issued-by-three-owners"
status = "authored"
claimed = "2026-08-15"

[[reservation]]
number = 30
slug = "cloud-commerce-is-composed-from-complete-domain-owners"
status = "authored"
claimed = "2026-08-15"

[[reservation]]
number = 31
slug = "an-authority-cutover-is-sealed-by-its-own-evidence"
status = "authored"
claimed = "2026-08-15"

[[reservation]]
number = 32
slug = "unobserved-is-unknown-never-absent"
status = "authored"
claimed = "2026-08-22"

[[reservation]]
number = 33
slug = "sales-authority-stops-at-an-accepted-quote"
status = "authored"
claimed = "2026-08-22"

[[reservation]]
number = 34
slug = "media-observations-own-provider-reports-not-attribution"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 35
slug = "sites-own-local-revisions-not-hosting"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 36
slug = "inventory-owns-stock-ledger-traceability-and-valuation"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 37
slug = "assets-own-durable-units-not-finance-or-product-extensions"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 38
slug = "network-capabilities-decompose-sub-first"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 39
slug = "positioning-owns-location-evidence-not-business-consequences"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 40
slug = "sub-vnext-parity-capabilities-have-narrow-independent-owners"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 41
slug = "accounting-owns-posting-and-ledger-evidence"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 42
slug = "payables-owns-supplier-liabilities-not-payment-execution"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 43
slug = "analytics-owns-projections-not-domain-facts"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 44
slug = "banking-owns-observations-matching-and-reconciliation"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 45
slug = "tax-policy-is-data-and-returns-are-evidence"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 46
slug = "payroll-owns-calculations-and-liabilities"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 47
slug = "expenses-own-spend-evidence-not-payment"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 48
slug = "finance-owns-asset-books-and-accounting-consequences"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 49
slug = "documents-own-controlled-versions-records-own-declared-evidence"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 50
slug = "procurement-owns-sourcing-and-purchasing-decisions"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 51
slug = "projects-own-work-structure-not-product-consequences"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 52
slug = "conversations-own-threads-not-transport-or-workforce-policy"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 53
slug = "surveys-own-feedback-evidence-not-subject-consequences"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 54
slug = "work-orders-own-physical-execution"
status = "authored"
claimed = "2026-08-20"

[[reservation]]
number = 55
slug = "web-analytics-owns-first-party-observations"
status = "authored"
claimed = "2026-08-21"

[[reservation]]
number = 56
slug = "campaigns-own-outbound-progression"
status = "authored"
claimed = "2026-08-23"

[[reservation]]
number = 57
slug = "the-vendor-control-plane-composes-existing-owners"
status = "authored"
claimed = "2026-08-23"

[[reservation]]
number = 58
slug = "isp-essential-domains-have-ten-independent-owners"
status = "authored"
claimed = "2026-08-23"

[[reservation]]
number = 59
slug = "availability-is-chosen-and-ownership-moves-are-distinct"
status = "authored"
claimed = "2026-08-24"

[[reservation]]
number = 60
slug = "the-response-clock-is-a-shared-owner"
status = "authored"
claimed = "2026-08-24"

[[reservation]]
number = 61
slug = "a-payout-is-erps-decision-and-its-provider-is-a-binding"
status = "authored"
claimed = "2026-08-24"

[[reservation]]
number = 62
slug = "modules-own-metric-definitions-assemblies-own-exporters"
status = "authored"
claimed = "2026-08-24"

[[reservation]]
number = 63
slug = "treasury-owns-the-payment-instruction-not-the-batch"
status = "authored"
claimed = "2026-08-24"

[[reservation]]
number = 64
slug = "semantic-content-identity-is-not-a-row-counter"
status = "authored"
claimed = "2026-08-25"

[[reservation]]
number = 65
slug = "mobile-clients-are-composed-applications"
status = "authored"
claimed = "2026-08-26"

[[reservation]]
number = 66
slug = "the-database-runtime-is-instantiated-not-shared"
status = "authored"
claimed = "2026-08-26"

[[reservation]]
number = 67
slug = "a-credential-holding-client-tears-down-atomically"
status = "authored"
claimed = "2026-08-26"

[[reservation]]
number = 68
slug = "the-kernel-owns-the-capability-artifact-grammar"
status = "authored"
claimed = "2026-08-26"

[[reservation]]
number = 69
slug = "mobile-authentication-federates-to-the-existing-identity-provider"
status = "authored"
claimed = "2026-08-26"

[[reservation]]
number = 70
slug = "deployment-is-a-stateless-versioned-foundation"
status = "authored"
claimed = "2026-08-27"

[[reservation]]
number = 71
slug = "a-recovery-bundle-carries-the-role-closure"
status = "authored"
claimed = "2026-08-30"

[[reservation]]
number = 72
slug = "a-displaced-executor-retires-on-proved-evidence"
status = "authored"
claimed = "2026-08-31"

# ---------------------------------------------------------------------------
# Claimed off `main`. These are the numbers a new allocation must not take.
# Each carries the branch that holds it, so the next author can see a claim
# that `docs/adr/` cannot show them. The branch names are DECLARED — see
# `[[branch_collision]]` below for why they cannot be detected.
# ---------------------------------------------------------------------------

[[reservation]]
number = 73
slug = "a-tax-snapshot-carries-the-components-tax-computed"
status = "reserved"
claimed = "2026-09-03"
where = "docs/tax-billing-evidence-path, and the two branches stacked on it"
note = """Uncontested. One decision on three stacked local branches \
(`docs/tax-billing-evidence-path`, `docs/shadow-evidence-and-release-state-rules`, \
`docs/claude-md-index-rules-43-52`), which is one claim carried forward, not \
three claimants."""

[[reservation]]
number = 74
slug = "tax-owns-determination-billing-renders-it"
status = "reserved"
claimed = "2026-09-04"
where = "docs/tax-ownership-decision"
note = """Michael's ruling, 2026-09-04: Tax keeps 0074 because its commit was \
created first — `0f8b0746` at 09:39:14 against the Foundation pair's \
`92f3b977` at 09:40:58. That is a fact about the two commits rather than a \
priority rule invented after the collision, and the extra renaming it forces \
is preferable to inventing one. AGENTS.md rule 40's tie-break ("the first \
decision to reach `main` keeps it") gave no answer here, because neither \
claimant had reached `main`."""

[[reservation]]
number = 75
slug = "recover-is-its-own-authorized-operation"
status = "reserved"
claimed = "2026-09-04"
where = "docs/foundation-recover-adrs"
note = """Renumbered from 0074 by the same ruling. Six lines changed by \
meaning, not by text replacement: this record and 0076 cite each other, and \
the files also cite ADR-0018, 0023, 0070 and 0071, which a regex would have \
corrupted."""

[[reservation]]
number = 76
slug = "the-six-performed-comparisons-become-declarable-names"
status = "reserved"
claimed = "2026-09-04"
where = "docs/foundation-recover-adrs"
note = "Renumbered from 0075 by the same ruling."

# ---------------------------------------------------------------------------
# Pre-existing branch collisions, DECLARED. A number already `authored` on
# `main` that an unmerged branch also spells with a DIFFERENT decision. These
# are not `contested` rows — `main` holds the number and the decision on it is
# settled; what is unsettled is the branch, which must renumber before it can
# merge.
#
# This block is DECLARED, NOT DETECTED. A checker reading one worktree cannot
# see another branch, and calling this coverage would be a claim the mechanism
# cannot honour. `branch_claim_findings` in the checker WILL compare this block
# against a real ref scan, but only where the refs are actually present, and it
# reports the coverage it had rather than passing silently over an empty one.
#
# THE AUTHORITY IS THE MERGE-BASE RESERVATION GATE, NOT THE REF SCAN.
# `unreserved_authorings` runs against the branch's merge base on every CI run
# of every branch, and the merge gate
# `test_adr_numbering.py::test_every_adr_number_is_unique` fails the moment a
# colliding document actually lands. The all-ref scan is a SUPPLEMENTARY AUDIT:
# CI usually sees one branch and a shallow ref set, so a scan there can only
# ever be a sample. It is wired to state its own coverage and to declare
# NON-EXECUTION when the coverage is inadequate, so that "zero collisions
# found" can never be read as "only one branch was visible".
#
# Two-directional per ADR-0018: this block may not grow, and it may only shrink
# in the change that reconciles the entry it removes.
#
# Not listed here, deliberately: thirty-one cases where a branch spells a
# number that `main` later gave to the SAME decision — `campaigns-own-outbound-
# progression` sitting at 0032 on 303 refs while `main` has it at 0056, and so
# on. Those are the old rule's renumber-before-merge working as designed on
# decisions that have since landed. They are history, not claims, and putting
# them here would bury the seven entries that are. They are preserved WITH
# their equivalence evidence in `docs/adr/historical-renumberings.toml`,
# deliberately a separate file and a separate pin, so that the live queue stays
# seven entries long and stays useful.
#
# ---------------------------------------------------------------------------
# DISPOSITION — Michael's ruling, 2026-09-04 (branch disposition). This
# vocabulary REPLACES the first classification vocabulary of the same day.
#
#   "There are no active collision repairs to perform. What remains is
#    preventing abandoned history from accidentally becoming authority."
#
# That is the whole purpose of this block now. Not one of the seven needs a
# number, a rebase or a renumber; every one of them needs to be unable to
# become authority by accident — by being merged, revived, cited or read as
# current by someone who never saw this file.
#
# The set is CLOSED and has SIX values. The checker reads it, and an invented
# seventh fails:
#
#   active                 intended for promotion. Reserve a NEW number on
#                          `main` FIRST, rebase the claimant, rename the ADR,
#                          update its citations one at a time BY MEANING,
#                          re-run the semantic citation verification, and
#                          confirm that unrelated references to the same number
#                          are unchanged. `main`'s ADR always keeps the number.
#   dormant                a CANDIDATE, not a corpse. The problem it addresses
#                          may still be real; the branch is simply not being
#                          worked. It is the one non-active value that keeps a
#                          future, so its `successor_or_resumption` states the
#                          conditions under which it may be picked up again —
#                          and resuming it is a new allocation, never a revival
#                          of the number or the ref.
#   superseded             a later decision replaced it. The successor is named
#                          and the branch is never merged.
#   closed_unmerged        its pull request was closed without merging, and
#                          that closure IS the disposition. Requires `pr`,
#                          `pr_state = "closed_unmerged"` and `pr_closed_at`.
#   never_merge            ruled unmergeable, whatever its pull-request state
#                          says. Stronger than `closed_unmerged`, because a
#                          closed pull request can be reopened by anyone with
#                          the button and a branch on `origin` can be merged
#                          without a pull request at all.
#   historical_equivalent  the SAME decision already lives on `main`, under
#                          another number or as the shipped contract it
#                          decided. `represented_by` names the number that
#                          carries it, and semantic identity was confirmed by
#                          reading BOTH documents — not by matching topics. The
#                          duplicate branch is retired, not renumbered.
#
# EVERY NON-ACTIVE ROW RECORDS ITS SUCCESSOR OR ITS EXPLICIT RESUMPTION RULE.
# `successor_or_resumption` is MANDATORY on every row whose disposition is not
# `active`, and the checker REFUSES a row that omits it. This is the defect the
# ruling exists to prevent: a branch classified as not-active with nothing
# saying where its decision went or on what terms it could come back is exactly
# the abandoned history that later gets read as authority, because the only
# thing a reader can then do is open the branch and believe it. A disposition
# is where the work went; the successor or the resumption rule is what a reader
# does next. A row is not classified until it carries both.
#
# A BRANCH NAME IS EVIDENCE OF STORAGE LOCATION, NOT INTENT. `archive/*` says a
# commit was parked somewhere; it does not say the decision was abandoned, and
# `rescue/*` does not say it was worth rescuing. This is why the previous
# `rescue-only` value is gone: it named where a commit lives, and Michael's
# ruling is that the row must name what is to be DONE with it. The two archive
# refs below now carry three different dispositions between them, which is only
# possible once the name stops being the classifier.
#
# WHAT THE THREE RETIRED VALUES BECAME:
#
#   `pushed-open`         is not a disposition at all. Whether a branch is on
#                         `origin` and whether its pull request is open are
#                         EVIDENCE — recorded structurally in `pr`/`pr_state`
#                         — and the disposition states intent. A pushed, open
#                         claimant is `active` if it is meant to land and
#                         `never_merge` if it is not; the row says which.
#   `rescue-only`         splits by intent: `dormant` where the lane may be
#                         resumed, `superseded`/`never_merge` where it may not.
#   `represented-on-main` is renamed `historical_equivalent`, unchanged in
#                         meaning, and keeps `represented_by`.
#
# COORDINATES BEFORE DELETION. A remote branch that must never merge is safest
# deleted, and a deleted branch is unreachable history unless its coordinates
# were written down first. `remote_deletion_authorized = true` states that
# Michael has authorized deleting the remote ref, and the checker then REQUIRES
# `tip`, `rescue_ref` and `rescue_tag` on the same row — so the authorization
# cannot be recorded before the commit is durably reachable without it. NOTHING
# HERE DELETES A REF; the register records, the human deletes.
#
# FIELDS. `number`, `main_slug`, `branch_slug` and `where` identify the
# collision; `disposition` states intent; `evidence` states what was READ (ref
# tips and dates, whether the ref is on `origin`, pull-request state, and what
# `main` does or does not carry — a bucket asserted with no reading is a guess
# about a branch name); `successor_or_resumption` is mandatory off `active`;
# `represented_by` belongs to `historical_equivalent` alone; `tip` is the full
# forty-character commit; `document` is the ADR path on the branch, whose
# number must equal `number`; `pr`, `pr_state` and `pr_closed_at` record the
# pull request; `rescue_ref`, `rescue_tag` and `remote_deletion_authorized`
# record durable reachability. Every one of them is READ by the checker — a
# field nothing reads is a claim nothing checks.
#
# As disposed on 2026-09-04, NONE of the seven is `active`. `active` and
# `closed_unmerged` are nevertheless live vocabulary, exercised by the planted
# proofs in the checker, because the next collision may well be one.
# ---------------------------------------------------------------------------

[[branch_collision]]
number = 19
main_slug = "party-identity-follows-the-archetype"
branch_slug = "a-decision-must-be-askable"
where = "rescue/stash-19-askable-licence-wip-20260902"
tip = "b7631cefe1078f2cb3b3759159183beb39440a64"
document = "docs/adr/0019-a-decision-must-be-askable.md"
disposition = "dormant"
evidence = """LOCAL ONLY — `git rev-parse origin/rescue/stash-19-askable-\
licence-wip-20260902` fails; one ref carries the file. Tip `b7631cef`, \
2026-09-02, subject `rescue(stash): preserve stash@{19} (askable-licence-wip)` \
— a preserved stash, not a lane. Its merge base with `main` is `c8237bd9` \
(2026-08-11), three weeks behind. The decision has NOT landed: `main` carries \
no ADR spelling the askable rule, no `tests/architecture/test_askable_\
decisions.py`, and `app/features/licensing/service.py` on `main` exposes no \
side-effect-free predicate."""
successor_or_resumption = """DORMANT — a CANDIDATE, NOT DEAD (Michael's \
ruling, 2026-09-04). No successor: nothing on `main` decides whether a \
decision must be askable, so there is no record to point a reader at, and the \
question is still open rather than answered elsewhere. RESUMPTION RULE, in \
order: (1) revalidate the PROBLEM — that a decision which cannot be asked \
without performing it is untestable — against today's code, because a \
three-week-old stash is evidence of what someone was doing, not of what is \
still true; (2) choose the correct OWNER for it, which is a fresh decision \
and may well not be `licensing`; (3) reserve a FRESH number from `next_free` \
like any other author. DO NOT revive ADR-0019's number — `main` authored it \
for the party archetype — and DO NOT resume this branch directly: it is a \
stash tip three weeks behind `main`, so what is carried forward is the \
argument, re-authored, not the ref."""

[[branch_collision]]
number = 32
main_slug = "unobserved-is-unknown-never-absent"
branch_slug = "collections-assesses-identity-and-requests-actions"
where = "feat/orders-eligibility-sales-handoff and 17 further refs"
document = "docs/adr/0032-collections-assesses-identity-and-requests-actions.md"
disposition = "historical_equivalent"
represented_by = 20
evidence = """Eighteen refs carry it in two blob revisions \
(`a3f91270` on fifteen, `fd3a2f52` on three); tips run 2026-08-18 to \
2026-09-02 and every lane is dead — the only pull request among them, #273 on \
`agent/network-suite-reconciled`, is CLOSED unmerged, and the rest are \
`salvage/`, `archive/` and rescued-stash refs. SEMANTIC IDENTITY CONFIRMED by \
reading the branch document against what `main` ships. Branch section 1 \
(`AssessCollectionExposureV1` carries identity, scope and trigger provenance \
and never money) is `packages/dotmac-collections/src/dotmac_collections/\
contracts.py` on `main`, field for field, docstring `Identity and provenance \
only; current money is always reread`. Section 2 (`ReceivablesReader` \
protocol, in-memory fake, reread at every decision point) is \
`receivables.py`'s `ReceivablesReader` and `FakeReceivablesReader`. Section 3 \
(`CollectionActionRequestedV1` is the one outbound name, typed receipt, no \
`ConsequenceRequestV1` alias) is `actions.py`. The module's own \
`EXTRACTION.toml` states that contract and cites `docs/adr/0020-billing-owns-\
operational-receivables.md`, which is the ADR on `main` that carries the \
decision. ONE DIVERGENCE, an evolution rather than a different decision: \
branch section 5 (`The initial package is tenant-plane only`) declares \
`platform_tables=()` for revision 1 and says so in as many words, while \
`main`'s `dotmac-collections` manifest declares both planes with \
`supported_plane_sets` naming tenant, platform and both."""
successor_or_resumption = """SUCCESSOR: ADR-0020 on `main` \
(`docs/adr/0020-billing-owns-operational-receivables.md`), which carries this \
decision, plus its `Historical amendment, 2026-09-04` recording the \
tenant-plane-only revision 1 and the dual-plane evolution under ADR-0023. \
NEVER MERGE: merging would put a second ADR-0032 in `docs/adr/` and a second \
authoritative record for one decision, which is precisely what the amendment \
was written instead of. Michael's ruling, 2026-09-04: "ADR-0032 and main \
ADR-0020 are semantically the same decision. The later dual-plane form is an \
evolution, not another owner. A new ADR would create two authoritative \
records for one decision." Retire the duplicate; it is not a renumbering. If \
the branch's prose is wanted on `main` as its own record, that is a NEW \
reservation from `next_free`, never this branch's number."""

[[branch_collision]]
number = 32
main_slug = "unobserved-is-unknown-never-absent"
branch_slug = "integrator-executes-approved-provisioning-commands"
where = "origin/archive/worktrees/2026-08-25/starter-managed-services-working"
tip = "5dbc80e4e59dbc4216b32e3aa0742d38969bd841"
document = "docs/adr/0032-integrator-executes-approved-provisioning-commands.md"
disposition = "superseded"
evidence = """One ref, an ARCHIVE ref whose tip `5dbc80e4` (2026-08-26) is \
literally `chore(archive): preserve starter-managed-services-working state` — \
storage location, which says nothing about intent. Not landed: \
`ConnectorMode.PROVISION` and `ProvisionPlugin` appear nowhere in `main`'s \
`packages/dotmac-integration`, where `ConnectorMode` (`spi.py`) is a closed \
three-member union — `INGRESS`, `POLL`, `DELIVERY` — each obliging the engine \
to supply machinery a plugin cannot bring. `main`'s ADR-0068 has already ruled \
on this exact branch — "The archive branch minted its own numbers; they are \
not reservations" — so the number is not a claim. READ AND RECORDED, because \
it looks like a contradiction and is not: the CURRENT provisioning lane \
`incubate/integration/slice-18-provision-contract` @ `a0a6f1d5` (2026-09-03, \
`feat(integration): add provider-neutral provisioning SPI`) does spell \
`ConnectorMode.PROVISION` and `ProvisionPlugin` again, at SPI 1.5, but as a \
provider-neutral plan/apply/observe/cancel contract \
(`provision_contract.py`: the product owns desired state and the ordered plan; \
the connector may execute steps but may not insert, remove, reorder or \
rewrite them) reached through `CapabilityDeclaration.modes`. The names \
survive; the archived branch's shape does not."""
successor_or_resumption = """SUPERSEDED / NEVER MERGE (Michael's ruling, \
2026-09-04). SUCCESSOR: the newer provisioning SPI — capability declarations \
and provider-neutral ports — currently on \
`incubate/integration/slice-18-provision-contract` @ `a0a6f1d5`. REASON, as \
ruled: reviving a fixed connector mode would recreate the provider/mode \
vocabulary the Integrator's plugin architecture exists to avoid. RESUMPTION \
RULE: none — do not resume the branch. Preserve any UNIQUE tests or findings \
by extracting THEM onto the current provisioning lane, one at a time and on \
their own merits; the ref is read-only evidence and is never restored \
wholesale. No number is spent and no citation is renumbered."""

[[branch_collision]]
number = 33
main_slug = "sales-authority-stops-at-an-accepted-quote"
branch_slug = "digital-media-owns-reusable-assets-rights-and-renditions"
where = "archive/worktrees/2026-08-25/digital-media (local and origin)"
tip = "b4403ac502581ba3f68e599e8f10549569f45d0f"
document = "docs/adr/0033-digital-media-owns-reusable-assets-rights-and-renditions.md"
disposition = "superseded"
evidence = """Two refs, local and `origin`, one tip `b4403ac5` (2026-08-25) \
whose subject is `chore(archive): preserve digital-media worktree state` — \
again storage, not intent. Not landed: `main` has no \
`packages/dotmac-digital-media` and no DAM ADR — `dotmac-content` and \
`dotmac-media-observations` are different owners, which the branch document \
says in its own Context. A CURRENT lane exists: DM1, \
`incubate/digital-media/slice-1` @ `b3646fab` (2026-09-04), preserved at \
`refs/rescue/digital-media-slice-1-20260904`, which already carries \
`incubation/digital-media/ARCHIVE-REFUSALS.md` — a named, file-by-file record \
of what it read from this archived package (13 files, read with `git show`, \
never restored) and what it REFUSED. That file is a refusal list, not the \
comparison below: it says what DM1 declines to own, not whether DM1 preserves \
every unique property this branch had.

COMPARISON RUN 2026-09-04, read-only through `git show`, judged on behaviour \
and contract shape rather than filenames, shared class names or ancestry. \
Archive: `packages/dotmac-digital-media`, 13 files, `contracts.py` 344 lines, \
`models.py` 816, `service.py` 1906, 15 tenant tables, one lineage \
`dm_0001_digital_media.py`. DM1: `incubation/digital-media/src/\
dmi_digital_media` — five modules, no model, no migration, no schema, \
in-memory repositories behind Protocols — plus six test modules.

(1) ASSET IDENTITY. Archive MINTS it (`models.py:105` `uuid_pk`), and an \
asset cannot exist outside a `MediaLibrary` (`models.py:107`, FK \
`models.py:60-68`); the current-revision pointer is held to a revision of the \
same tenant AND asset by a composite FK (`models.py:69-79`). DM1 takes a \
CALLER-supplied id, refuses reuse with `AssetIdentityConflict` \
(`authority.py:171`), keeps the module generator-free, and holds description \
apart from identity (`authority.py:200`, `contracts.py:252`). Both guarantee \
allocate-once, never re-point. VERDICT: EQUAL on the property, divergent on \
who mints; DM1 additionally proves no `uuid4`/clock anywhere \
(`tests/test_architecture.py:171`) and that two tenants may hold one asset id \
independently (`test_asset_identity_and_revision.py:311`). The library \
container is refused organisation, not lost identity.

(2) SOURCE REVISION. Archive identifies a revision by surrogate `uuid_pk` \
ordered by `revision_number = max+1` (`service.py:275-297`); resubmitting \
identical bytes silently creates a SECOND revision row — `add_revision` \
dedupes nothing — and sealing bytes also reset an `available` asset to \
`ingesting` (`service.py:301-306`), a product policy inside an identity \
operation. Immutability is enforced in the CATALOG by a `BEFORE UPDATE OR \
DELETE` trigger (`dm_0001_digital_media.py:896-938`). DM1 identifies a \
revision by its byte evidence: `dmrev1:` over file ref, checksum, byte length \
and media type (`identity.py:73`), so identical evidence REPLAYS \
(`authority.py:224`) and the same evidence under different context is an \
`EvidenceConflict` rather than a silent rewrite; immutability is structural — \
frozen slotted types, append-only store, no operation able to name a revision \
for edit (`contracts.py:281`, `repositories.py:46-55`, \
`test_asset_identity_and_revision.py:92,122`). VERDICT: SUPERSET in Python, \
GAP in the catalog — see (7).

(3) BYTE CUSTODY. Both hold that Files owns bytes. Archive stores an opaque \
file UUID plus a supplied checksum (`models.py:178-179`) and forbids \
`file_path`/`object_key`/`bucket` columns \
(`tests/architecture/test_digital_media_module.py:56`), but the checksum \
carries NO algorithm and its VALUE is lowercased (`service.py:268`) — an \
encoding assumption by a module that enumerates no algorithm. DM1 makes \
`FileObjectReference` a single identifier field (`contracts.py:183`), pairs \
every checksum with its algorithm (`contracts.py:198`), case-folds the \
algorithm NAME only (`authority.py:101`), and returns NOT_COMPARABLE rather \
than DIFFERS across algorithms (`authority.py:118`). VERDICT: SUPERSET, and \
enforced harder — an exact stdlib import allowlist, no annotation mentioning \
`bytes`, nothing that opens/reads/streams \
(`tests/test_architecture.py:106,210,230`).

(4) RENDITION PROVENANCE. Archive binds one exact source revision id AND its \
checksum (`service.py:950-952`) and dedupes by a `request_fingerprint` that \
is `sha256(json.dumps(..., default=str))` in a `String(64)` column \
(`models.py:526`) — algorithm invisible at the boundary, encoding not \
length-prefixed, no typed absence. It also carries a closed 11-member \
`RenditionKind` (`contracts.py:62`), a job lifecycle, and a \
`rendition_is_stale` rule that compares recipe/engine VERSION while ignoring \
recipe/engine CODE (`service.py:1206`), so switching engines outright left a \
rendition fresh. DM1 DERIVES the identity from asset plus complete provenance \
(`identity.py:106`) over a length-prefixed, typed-absence, sorted canonical \
encoding (`canonical.py:45,64,78`), refuses provenance whose checksum does \
not equal the sealed revision's (`authority.py:609`), and always carries a \
repair plan (`contracts.py:373`, `authority.py:334`). VERDICT: SUPERSET — the \
archive's "repairable, never replaces source" claim is preserved and \
strengthened; the closed kind, the job states and the staleness policy are \
refused delivery/transformation behaviour.

(5) CONSUMER OWNERSHIP. Archive DECIDES: `may_access` with grants, effects \
and hardcoded nearest-scope ranks (`service.py:1559-1616`), and \
`evaluate_usage` returning `allowed: bool` over territory/channel/purpose/\
window/embargo/commercial/modification and a `release_evidence_valid` boolean \
it stored NOT NULL (`service.py:778-844`, `contracts.py:166-213`) — one \
function answering both "is this licensed" and "is this publishable". DM1 \
records only WHICH licence applies (`contracts.py:425`, `authority.py:476`), \
several may apply and none wins, and no operation accepts a proposed use or \
returns a permission — proved structurally with a planted-defect sensitivity \
proof (`test_rights_are_references.py:109,125,137,153`). VERDICT: DIVERGENCE \
SETTLED BY RULING, not a live disagreement: DM1's \
`incubation/digital-media/BOUNDARY-RULING.md` is Michael's ACCEPTED ruling of \
2026-09-03, point 3, that the consuming product decides permitted use. The \
archive's evaluator is refused, not lost.

(6) RETENTION BOUNDARY. The two lanes AGREE in position: ADR-0033:67-72 on \
this branch already says an exact revision becomes a record only through \
explicit declaration and that Records is then sole writer, which is exactly \
`BOUNDARY-RULING.md` point 4. They differ in surface: the archive still \
reached a media-side verdict, `evaluate_disposition` answering \
`archive_allowed`/`dispose_allowed` from `active_usage`, `current_revision` \
and a supplied `records_allows_disposition` (`service.py:1850-1871`), while \
DM1 has NO retention, hold or disposition surface at all and proves the \
vocabulary cannot accrete, with a bite-and-non-bite proof \
(`test_records_boundary.py:45-61`). DM1's only deletion is \
`discard_projection` (`authority.py:465`), safe because the projection is \
re-derivable. VERDICT: EQUAL in position, DM1 NARROWER in surface; the \
positive half — that a declaration names one exact immutable revision — is \
PROSE on both sides in DM1's lane, because DM1 exposes no declaration seam to \
enforce.

(7) SENSITIVITY TESTS. DM1 is strictly ahead on detector discipline and \
strictly behind on the catalog. Archive guards: a hand-maintained denylist of \
provider roots, forbidden columns and string canaries \
(`test_digital_media_module.py:56,95`) — the AGENTS.md rule 25 extent defect, \
silent on the next provider — with NO planted-defect proof anywhere, plus \
`test_services_do_not_own_transactions` (:134) and unit tests that ran on \
SQLite with `schema_translate_map`, where the immutability they named could \
not be exercised. DM1 replaces the denylist with an exact allowlist and adds \
four explicit bite proofs \
(`test_architecture.py:195,411`, `test_rights_are_references.py:153`, \
`test_records_boundary.py:49`). BUT the archive holds two enforcements DM1 \
has none of: forced-RLS and cross-tenant read/write canaries on a real \
migrated Postgres (`tests/test_digital_media_isolation.py:122,142`) and a \
revision UPDATE/DELETE refusal proved against the live trigger (:186-237). \
DM1's own `INCUBATION-NOTE.md:174-182` states both as known limits, and \
`PROMOTION.md` items 5 and 6 already carry them as binding promotion \
conditions. VERDICT: GAP AT THE ENFORCEMENT LAYER, not at the behaviour \
layer, and not transplantable into a lane whose charter forbids models, \
migrations and `mod_*` schema."""
successor_or_resumption = """SUPERSEDED / NEVER MERGE (Michael's ruling, \
2026-09-04, applied after the comparison he ordered was run). SUCCESSOR: DM1, \
`incubate/digital-media/slice-1` @ `b3646fab`, preserved at \
`refs/rescue/digital-media-slice-1-20260904` and tag \
`rescue-digital-media-slice-1-20260904`. REASON: across the seven recorded \
properties DM1 is a superset on asset identity, source revision, byte custody \
and rendition provenance; equal-and-narrower on the retention boundary; and \
ahead on consumer ownership by an ACCEPTED ruling \
(`incubation/digital-media/BOUNDARY-RULING.md`, 2026-09-03) that removes \
rights evaluation and access decisions from Digital Media altogether. The \
three other outcomes were rejected explicitly: the archive is not merely \
dormant, because the comparison has now been run; there is no SEMANTIC \
DISAGREEMENT needing a ruling, because the one place the lanes make opposite \
claims — who decides whether a use is permitted — is already adjudicated \
against the archive; and no transplant is owed onto DM1 today, because the \
one thing the archive enforces and DM1 does not is a persistence-layer \
guarantee the incubation charter forbids building. RESUMPTION RULE: none — do \
not resume, merge, cherry-pick or check out this branch. TRANSPLANT DEBT, to \
be discharged at DM1's promotion and nowhere earlier, matching `PROMOTION.md` \
items 5 and 6: (a) the append-only evidence guard — one schema-local \
`BEFORE UPDATE OR DELETE ... FOR EACH ROW` trigger function raising on the \
sealed-revision table, REVOKEd from PUBLIC, mirroring \
`dm_0001_digital_media.py:896-938`; and (b) its two real-Postgres canaries — \
per-table ENABLE plus FORCE row level security with a cross-tenant \
read/write canary, and an UPDATE/DELETE refusal exercised against the live \
trigger, mirroring `tests/test_digital_media_isolation.py:122,142,186`. \
Take the SHAPE, never the archive's schema: its 15 tables carry the refused \
rights, access, collection, annotation, saved-selection and usage-context \
models, and `ARCHIVE-REFUSALS.md` must survive the port intact. Also unique \
to this ref and NOT carried by DM1 as a file: \
`docs/inventories/digital-media-sources.md` (175 lines) and the \
`EXTRACTION.toml` eleven-repository audit with pinned source revisions and \
`contract_consumers = []` (:55). That is evidence, not behaviour; ADR-0006 \
rule 22 requires the dossier to be RE-DERIVED at current revisions at \
promotion, so it is read from this ref and never copied forward. No number is \
spent: ADR-0033 on `main` is `sales-authority-stops-at-an-accepted-quote`, \
and this branch document keeps no reservation."""

[[branch_collision]]
number = 33
main_slug = "sales-authority-stops-at-an-accepted-quote"
branch_slug = "exact-managed-service-connectors-are-authorized"
where = "origin/archive/worktrees/2026-08-25/starter-managed-services-working"
tip = "5dbc80e4e59dbc4216b32e3aa0742d38969bd841"
document = "docs/adr/0033-exact-managed-service-connectors-are-authorized.md"
disposition = "superseded"
evidence = """Same archive ref and same archival tip as ADR-0032 \
`integrator-executes-approved-provisioning-commands` above; the trio was \
authored together on 2026-08-17 and cites itself across the three numbers. \
Not landed: none of the seven distributions it authorizes \
(`dotmac-connector-contabo`, `-keycloak-admin`, `-mailcow`, `-nextcloud`, \
`-dotmac-erp`, `-dotmac-academy`, `-dotmac-host-agent`) exists on `main`, and \
`docs/inventories/managed-service-connector-sources.md` is not there either. \
Covered by ADR-0068's ruling that this branch's numbers are not reservations. \
The structural obsolescence is visible in the refs: EIGHT contract-catalogue \
lanes exist today — `incubate/contracts-{collaboration,domains,email,host,\
identity,infrastructure,suite,voice}/slice-1`, all cut 2026-09-03, \
`contracts-suite` being the composition — against a document that authorizes \
seven named distributions and freezes the list."""
successor_or_resumption = """SUPERSEDED / NEVER MERGE (Michael's ruling, \
2026-09-04). SUCCESSOR: the current train — eight catalogues, owner-local \
canonical artifacts, Suite composition, and the newer release-enrollment \
design. REASON, as ruled: a SEVEN-DISTRIBUTION AUTHORIZATION RECORD IS \
STRUCTURALLY OBSOLETE, even where individual prose in it remains useful; the \
document's value was the exact list, and the exact list is wrong. RESUMPTION \
RULE: none — do not resume the branch. Individual arguments may be re-used by \
whoever writes the current catalogues, on their own merits and under their \
own numbers; the ref is read-only evidence. No number is spent."""

[[branch_collision]]
number = 34
main_slug = "media-observations-own-provider-reports-not-attribution"
branch_slug = "managed-capability-contracts-are-separate-product-artifacts"
where = "origin/archive/worktrees/2026-08-25/starter-managed-services-working"
tip = "5dbc80e4e59dbc4216b32e3aa0742d38969bd841"
document = "docs/adr/0034-managed-capability-contracts-are-separate-product-artifacts.md"
disposition = "historical_equivalent"
represented_by = 68
evidence = """SEMANTIC IDENTITY CONFIRMED, and already adjudicated on `main`. \
`docs/adr/0068-the-kernel-owns-the-capability-artifact-grammar.md` names this \
exact document in its own front matter: "Supersedes the numbering of: the \
archived ADR-0034 on `archive/worktrees/2026-08-25/starter-managed-services-\
working`, whose number is held on `main` by ADR-0034 (media observations). \
The archive branch minted its own numbers; they are not reservations." \
Reading both documents: ADR-0068's Context is the branch document's Context \
verbatim — the same Mailcow/Nextcloud/Keycloak `upstream_third_party` framing, \
the same two-artifacts-answer-different-questions conclusion, and the same \
refusal to relabel them into one artifact to simplify a join. Same decision, \
not merely the same topic."""
successor_or_resumption = """SUCCESSOR: ADR-0068 on `main` \
(`the-kernel-owns-the-capability-artifact-grammar`), which carries this \
decision and says so in its own front matter. NEVER MERGE: the duplicate is \
retired, not renumbered, and nothing is renumbered on `main`. No resumption \
rule, because there is nothing left to resume — the decision is landed, and \
the branch document is superseded prose about an adjudicated question."""

[[branch_collision]]
number = 71
main_slug = "a-recovery-bundle-carries-the-role-closure"
branch_slug = "build-once-and-bind-the-environment-late"
where = "docs/adr-0071-build-once-bind-late (local and origin)"
tip = "3b409faf6555615e7ab580146f0288d9abd35926"
document = "docs/adr/0071-build-once-and-bind-the-environment-late.md"
disposition = "never_merge"
pr = 500
pr_state = "closed_unmerged"
pr_closed_at = "2026-08-29T19:10:16Z"
rescue_ref = "refs/rescue/adr-0071-build-once-bind-late-closed-unmerged"
rescue_tag = "rescue-adr-0071-build-once-bind-late-20260904"
remote_deletion_authorized = true
evidence = """THE ONE PUSHED CLAIMANT. Branch `docs/adr-0071-build-once-bind-\
late`, local and `origin`, both at tip `3b409faf6555615e7ab580146f0288d9abd\
35926`, authored 2026-08-29 19:26:48 +0100, subject `docs(governance): \
ADR-0071 — build once, bind the environment late`; the commit adds exactly two \
files, `AGENTS.md` and `docs/adr/0071-build-once-and-bind-the-environment-\
late.md` (blob `75f10f2e`). Its pull request #500 is CLOSED UNMERGED \
(2026-08-29T19:10:16Z), closed by Michael's own ruling that the ADR was in the \
wrong repository — "a fleet-wide standard cannot live inside one of the \
products it governs". Verified on `main`: `.dotmac/standards-profile.json` \
pins `governance_model.revision = \
4f80bd164137d34ff41f1866d7e748f2e664dee3`, and `main` carries no build-once \
ADR of its own — correctly, because the decision is not this repository's to \
hold. NOT `closed_unmerged` as a disposition, though its pull request is: a \
closed pull request can be reopened by anyone with the button, and a branch on \
`origin` can be merged with no pull request at all. The ruling is stronger \
than the pull-request state, so the disposition is `never_merge` and the \
closure is recorded beside it as evidence."""
successor_or_resumption = """SUCCESSOR, in two parts. FLEET-WIDE AUTHORITY \
MOVED TO `dotmac_governance`: the canonical build-once/bind-late ADR and its \
enforcing action live there, not here. STARTER SUCCESSOR: adoption in PR #504 \
(merged 2026-08-29T21:51:55Z), which pins the exact Governance revision in \
`.dotmac/standards-profile.json` and \
`.github/workflows/engineering-standards.yml` — adoption only, no local ADR. \
NEVER MERGE, and no resumption: there is nothing to resume in this repository, \
because holding the decision here is the defect that closed #500. No number is \
spent and no citation is renumbered. WHAT REMAINED WAS COORDINATION, AND THIS \
ROW IS IT: while the branch sits on `origin`, a reviewer can reopen #500 or \
merge the ref without ever seeing this file, and `docs/adr/` would then hold \
two ADR-0071 documents. Michael authorized DELETING the remote branch once \
these coordinates are durable here, which they now are: the tip above, the \
local rescue ref `refs/rescue/adr-0071-build-once-bind-late-closed-unmerged` \
and the tag `rescue-adr-0071-build-once-bind-late-20260904`, both at that same \
commit, keep it reachable after the remote branch is gone. The deletion is not \
performed by this file or by any check in it; the register records, the human \
deletes."""
