{
  "$comment": [
    "Every distribution in this repository that declares a version nobody can",
    "install, with the reason that state is acceptable. Measured by",
    "`scripts/declared_publication_sweep.py`, guarded by",
    "`tests/architecture/test_declared_publication.py`.",
    "",
    "## Why this file exists at all",
    "",
    "`test_module_version_sync.py` proves a module's three version surfaces",
    "AGREE. It cannot prove the agreed version EXISTS. `dotmac-integration`",
    "reads 0.1.0a2 on all three surfaces and the version-identity guard passes,",
    "while the newest tag is dotmac-integration-v0.1.0a1 — internal consistency",
    "and publication are different questions, and only the first one had a",
    "guard. A consumer reading the repository, the changelog or",
    "docs/MODULE_CATALOG.md and pinning ==0.1.0a2 gets a resolver error.",
    "",
    "## What a row here does and does not mean",
    "",
    "A row means the state is KNOWN and someone has said why. It is not",
    "permission to leave it there, and it is emphatically not a reason to edit",
    "the declared version down to match the index: that would make the",
    "repository agree with the registry by discarding the work the number",
    "describes, and the changelog entry for the unpublished version would then",
    "belong to nothing.",
    "",
    "## Two directions, both failures",
    "",
    "A distribution entering this state without a row fails — that is a version",
    "silently promised to consumers. A row whose distribution has since been",
    "published ALSO fails and must be removed in the same change as the release,",
    "because a ledger that only grows stops describing anything (ADR-0018).",
    "",
    "## The oracle is a git tag",
    "",
    "The release workflows write a tag only after `verify-registry` has",
    "installed the exact published version from the private index and registered",
    "its manifest, so a tag is this repository's own assertion that a version is",
    "installable — stronger than 'an upload succeeded'. The index is not queried",
    "directly: that needs an authenticated URL, which makes the check",
    "un-runnable at PR time and offline, and a gate that cannot run is not a",
    "gate. The stated cost is that a publication whose tag step failed reads",
    "here as unpublished — which is exactly the state",
    "`recover-module-release.yml` exists to repair, so reporting it is correct."
  ],
  "unpublished": {
    "dotmac-auth-oidc": {
      "declared": "0.1.0a1",
      "reason": "DELIBERATE, and the strongest case in this file. `.github/release-adapters.json` lists `adapters: {}` — the lane was built and left shut (PR #180), because absence from the allowlist is the safety mechanism until the `dotmac_workspace` pilot has run a real login against an exact-SHA local wheel and supplied a shared, atomic StateStore. Unpublished is the INTENDED state; publishing it early is the defect this row guards against.",
      "state": "never-published"
    },
    "dotmac-imports": {
      "declared": "0.1.0a2",
      "reason": "UNRESOLVED — the worst of the four, and it needs Michael's ruling. The module is release-allowlisted, its entry pins kernel_floor 0.1.0a56, and NO dotmac-imports-v tag has ever been written: it has never been published in any version. Its own CHANGELOG says `0.1.0a1 — unreleased`, so the ADR-0025 import-run ledger is installable by nobody, and a consuming assembly cannot pin it. Either dispatch a release run or move the entry out of the allowlist; an allowlist row for a distribution nobody can install makes the allowlist read as a catalogue of available modules.",
      "state": "never-published"
    },
    "dotmac-integration": {
      "declared": "0.1.0a2",
      "reason": "OPEN, and owned by the release step of the Integrator programme rather than by this guard. 0.1.0a2 is the 2026-08-14 strictness fix (run_effect_once could never have run; 42 type errors ship in the published 0.1.0a1). The published 0.1.0a1 is therefore the only installable version and it carries a known-broken public function, while the repository, the changelog and the module catalogue all read 0.1.0a2. `dotmac_integrator` cannot pin the fix. Recorded as evidence; NOT to be repaired by editing the version.",
      "state": "declared-unpublished"
    },
    "dotmac-template-studio": {
      "declared": "0.2.0a3",
      "reason": "Not in any release lane — absent from release-modules.json and release-adapters.json alike — so no workflow can publish it and no consumer can pin it. It is the still-unlisted stand-in the allowlist gate's own sensitivity proof uses (`test_an_unallowlisted_package_cannot_be_resolved`), which makes unpublished the consistent state rather than an oversight. It is recorded rather than exempted because 0.2.0a3 is the third declared version of a package that has never had a first release.",
      "state": "never-published"
    }
  }
}
