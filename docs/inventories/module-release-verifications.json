{
  "$comment": "Producer-owned immutable coordinates for verified module publications. Rows are appended mechanically by the release recorder from the annotated tag and the exact build-once wheel carried into registry verification; source changes never predict future release hashes.",
  "schema": "ModuleReleaseVerifications.v1",
  "releases": []
}
