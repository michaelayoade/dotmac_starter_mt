<!-- GENERATED by dotmac_deployment_foundation.rehearsal.render_pending_document — do not hand-edit. -->

# Exposure rehearsal (Lane 3) — 0 of 16 executed and passed

**No Lane 3 receipt exists.** The Platform CP authorization issuer is not yet deployed, so no authorization run id exists. Gate item 9 binds three terms and the middle one is the authorized plan, so `build_receipt` correctly refuses to construct a receipt at all. Lane 3 executes once the issuer, the lease and the controller identity exist.

**How this maps to the adopted baseline.** The accepted count was *8 closed, 4 controller-dependent, 3 blocked, 1 vacuous*, stated in the OLD vocabulary where "closed" included hand measurements. In this vocabulary nothing is closed until a controller executed it, so the same facts render as **0 executed_passed, 7 hand_measured, 4 not_executed, 4 blocked, 1 vacuous**. Nothing was downgraded on the merits: items 13 and 15 were one "closed" each in the old count while only their positive-control halves were measured, and here item 13 is `blocked` because its negative half needs the fixture running. The publication-relevant number was always zero.

Historical hand measurements are retained below as supporting context. They are recorded as `hand_measured`, which **cannot satisfy publication**: a hand-driven step proves the operator can do it, not that the controller can. `vacuous` means the check ran against a fixture that could not exercise it.

| # | Item | Evidence from | Status | Detail |
|---|---|---|---|---|
| 1 | Apply under the product deployment lock | target | not_executed | The controller path exists (`exposure-apply --execute`) but has never driven a host. The 2026-08-29 apply was a shell command. |
| 2 | Pre-change snapshot (HostObservation) | target | not_executed | A pre-state was captured by shell, not by `ExposureTransaction.run`. |
| 3 | A non-recreating apply is REFUSED | target | not_executed | `refuse_non_recreating_apply` has unit coverage; it has never refused a real invocation. |
| 4 | Socket re-observation, per family | target | hand_measured | 2026-08-29: 18443 on 127.0.0.1 AND [::1]; 18445 on 127.0.0.1 only; no 0.0.0.0 and no [::]. |
| 5 | docker-proxy PID is NEW, host-ip correct | target | hand_measured | 2026-08-29: pids 1136702 / 1136709 / 1136725. The parser discarded pids until 2026-08-30, so this could not have been checked by code. |
| 6 | Firewall rules land in the right chain, terminal DROP present | target | vacuous | The old fixture published only loopback and none, so the derived plan was EMPTY and nothing was observed. The 2026-08-30 fixture derives four rules and makes this testable. |
| 7 | The inert v6 DOCKER-USER chain, captured with a zero counter | target | blocked | Needs the private publication the new fixture adds, executed on a leased host. |
| 8 | Rollback, provoked rather than simulated | target | not_executed | Teardown restored the pre-state; no PROVOKED transactional rollback was driven. |
| 9 | Descriptor == authorized plan == execution report | target | blocked | Three terms, and only two were ever measured. There is no authorized plan because nothing can issue one. |
| 10 | exposure = "none" emits no socket at all | target | hand_measured | 2026-08-29: 18444 absent from `ss`. |
| 11 | The target's closed-port behaviour, recorded | workstation | hand_measured | Target RSTs; re-measured 2026-08-30 at 5-8 ms per refusal. |
| 12 | The privileged-vantage refusal fires on a real probe | workstation | hand_measured | 2026-08-29: the workstation result was refused, an unenumerated vantage was refused, and the external vantage was accepted. |
| 13 | IPv6 external negative against a RUNNING service | probe | blocked | The 2026-08-29 negative is not reusable: the fixture containers are gone, so a refusal now measures an absent service. Re-opens with the fixture RUNNING. |
| 14 | IPv6 external positive control to THIS target | probe | hand_measured | Re-measured 2026-08-30: tcp/22 over IPv6 to the target, OPEN. |
| 15 | IPv4 external negative plus its positive control | probe | hand_measured | Positive control re-measured 2026-08-30 (tcp/22 over IPv4, OPEN); the negative half re-opens with the fixture running. |
| 16 | A private exposure reached from inside its source set | probe | blocked | Needs the private publication and a vantage inside its declared source set. |

## Tally

- `executed_passed`: 0
- `executed_failed`: 0
- `not_executed`: 4
- `hand_measured`: 7
- `blocked`: 4
- `vacuous`: 1

**Publication requires all 16 to be `executed_passed`.** This does not: 16 item(s) short.
