# Changelog

## 0.1.0a1 — declared 2026-08-21

- Add immutable, versioned synchronization contracts and envelopes.
- Bind the claimed source to an already authenticated application peer.
- Validate destination-owned JSON Schema before invoking the receiver.
- Derive a stable idempotency key and a payload-sensitive fingerprint.
- Keep transaction authority, transport and every domain consequence in the
  independently deployed destination application.
