# Compatibility — dotmac-support-access

Version `0.1.0a1` requires `dotmac-kernel>=0.1.0a87`, which allocates
`mod_supportaccess` and migration prefix `sup`. The package is platform-only and
is not composed by the Starter reference assembly.
