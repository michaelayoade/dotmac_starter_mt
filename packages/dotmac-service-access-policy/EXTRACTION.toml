schema_version = 1
package = "dotmac-service-access-policy"
classification = "optional-module"
status = "audit-complete"
source_mode = "product-first"
owner = "Per-service desired access decisions from operational policy inputs"
contract = "Own current per-service FUP, prepaid, collections and administrative policy observations plus the derived desired ALLOW/RESTRICT/DENY state. Consequences are scoped to the affected service. NOT customer/subscriber status, balance/collections ownership, AAA facts, network-device state or enforcement."
source_repositories = ["dotmac_sub", "dotmac_erp", "dotmac_starter_mt"]
source_paths = [
  "dotmac_sub:app/models/network_access.py",
  "dotmac_sub:app/services/network_access_policy.py",
  "dotmac_sub:app/services/fup.py",
  "dotmac_sub:app/services/collections.py",
  "dotmac_erp:app/models/subscription.py",
]
preserved_tests = [
  "dotmac_sub:tests/test_network_access_policy.py",
  "dotmac_sub:tests/test_fup.py",
  "dotmac_sub:tests/test_collections_service_scope.py",
  "dotmac_starter_mt:tests/unit/test_service_access_policy_service.py",
  "dotmac_starter_mt:tests/architecture/test_service_access_policy_module.py",
  "dotmac_starter_mt:tests/test_service_access_policy_isolation.py",
]
contract_consumers = []
candidate_consumers = ["dotmac_sub"]
composition_boundary = "FUP, prepaid, collections and admin adapters record typed observations; the resolver writes desired state. Network Access consumes that state through a typed port and is the only enforcer. No sibling module import is permitted."
inventory_evidence = ["docs/inventories/isp-essential-domain-sources.md"]
first_cutover = "Sub remains access-policy owner until input backfill and per-service desired-state shadow comparison complete before Network Access switches its reader."
shadow_and_drift = "Compare each service's active signals, precedence, desired access, reason and decision time; device/AAA state remains an external enforcement projection."
local_copy_retirement = "After cutover, reduce Sub parallel desired-access resolvers and direct policy-to-device writers to zero; Network Access remains the reconciler."
next_action = "Compose in Sub and shadow per-service decisions before switching Network Access to the typed desired-state reader."
