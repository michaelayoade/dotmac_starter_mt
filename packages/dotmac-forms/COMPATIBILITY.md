# Compatibility — dotmac-forms

Version `0.1.0a1` requires `dotmac-kernel>=0.1.0a87`, which allocates
`mod_forms` and migration prefix `fm`. The package is tenant-only and is not
composed by the Starter reference assembly.
