[tool.poetry]
name = "dotmac-network-assurance"
version = "0.1.0a1"
description = "Tenant network incident, impact, maintenance and SLA-evidence owner"
authors = ["DotMac"]
readme = "README.md"
packages = [{ include = "dotmac_network_assurance", from = "src" }]
include = [{ path = "src/dotmac_network_assurance/py.typed", format = ["sdist", "wheel"] }, { path = "src/dotmac_network_assurance/migrations/**/*", format = ["sdist", "wheel"] }]

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
dotmac-kernel = ">=0.1.0a73"
sqlalchemy = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
