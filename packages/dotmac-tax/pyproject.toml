[tool.poetry]
name = "dotmac-tax"
version = "0.1.0a1"
description = "Composable tax determination, statutory reporting and return-lifecycle owner"
authors = ["DotMac"]
readme = "README.md"
packages = [{ include = "dotmac_tax", from = "src" }]
include = [
  { path = "src/dotmac_tax/py.typed", format = ["sdist", "wheel"] },
  { path = "src/dotmac_tax/migrations/**/*", format = ["sdist", "wheel"] },
]

[tool.poetry.dependencies]
python = ">=3.12,<3.14"
dotmac-kernel = ">=0.1.0a85"
sqlalchemy = "^2.0"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
