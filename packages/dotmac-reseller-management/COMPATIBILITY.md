# Compatibility

`dotmac-reseller-management` 0.1.0a1 requires `dotmac-kernel >=0.1.0a83`.

This first contract has one tenant plane and requires the tenant scope catalog,
module database roles and kernel outbox relay prerequisites. It has no platform
plane and no product or provider dependency.
