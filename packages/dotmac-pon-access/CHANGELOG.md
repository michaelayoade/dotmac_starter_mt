# Changelog

## 0.1.0a1 — 2026-08-21

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `bfc112fc` by release run
`32482755879`. Publication is supply-chain evidence only; it composes no product
and moves no authority.

- Adds the tenant-only `mod_pon` lineage (`pn` revision prefix).
- Owns provider-neutral OLT/ONT/PON inventory, admission, assignment,
  commissioning, desired service state, observations, backup evidence and
  reconciliation.
- Exposes `versions_dir()` as the public installed-lineage locator.
