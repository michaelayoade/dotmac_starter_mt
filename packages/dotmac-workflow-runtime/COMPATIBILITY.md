# Compatibility — dotmac-workflow-runtime

Version `0.1.0a1` requires `dotmac-kernel>=0.1.0a87`, which allocates
`mod_workflow` and migration prefix `wr`. It has no dependency on Forms,
Fulfillment or Durable Timers.
