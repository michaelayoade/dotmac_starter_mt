# Changelog — dotmac-workflow-runtime

## 0.1.0a1 — 2026-08-22

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `8f52abc4` by release run `32570447051`.
The composition check registered this manifest alongside forms
against published kernel `0.1.0a88` before the tag was written. Publication
composes nothing into an assembly and adopts nothing.

- Port ERP's execution/retry evidence into a tenant-only resumable runtime.
- Add deterministic checkpoint order, expiring claims, bounded attempts and
  evidence-bound operator repair without importing a product effect handler.
