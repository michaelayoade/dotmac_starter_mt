schema_version = 1
package = "dotmac-durable-timers"
classification = "optional-module"
status = "audit-complete"
source_mode = "product-first"
owner = "Timer identity, generation allocation, scheduling and rescheduling, supersession, cancellation, current-generation verification, stale-trigger rejection, append-only timer history and terminal-history retention"
contract = "Given an explicit tenant or platform scope, an opaque (owner, entity_kind, entity_id, purpose) identity, a required timezone-aware due_at, a manifest-declared output code and optional opaque expected_source_version, atomically allocate the next generation, supersede the prior current generation, append history and enqueue one kernel outbox event whose available_at equals due_at. Cancellation returns one of Canceled, AlreadyFired, NothingScheduled or Stale and names both observed and current generations. Trigger acceptance locks and re-derives current identity state, records stale-rejection evidence, and returns observed_generation, current_generation and expected_source_version before the caller may run its effect. Retention never deletes a scheduled timer. The module declares and reuses outbox_relay.v1 for claiming, leasing, stale-lease recovery, retry, backoff, dead-letter handling and dispatcher privileges; it contains no due-row scanner, claim loop, retry engine, consumer decision or ambient clock read."
source_repositories = [
  "dotmac_erp",
  "dotmac_sub",
  "dotmac_starter_mt",
]
source_revisions = [
  "dotmac_erp:9d67c3990e01e20409ab118badb5dfdf7ce045a7",
  "dotmac_sub:4489ca1712f3c263d914f2af0ebfcf044aa70605",
  "dotmac_starter_mt:7e0543004864845f0035c9ec325e3f5064c281cc",
  "dotmac-kernel-v0.1.0a67:ed3ac864b350d4556808a69496f999f764682442",
]
source_paths = [
  "dotmac_erp:app/models/scheduler.py",
  "dotmac_sub:app/models/durable_timer.py",
  "dotmac_sub:app/services/runtime_durable_timers.py",
  "dotmac_sub:app/tasks/durable_timers.py",
  "dotmac_sub:app/models/subscription_lifecycle_schedule.py",
  "dotmac_sub:app/services/subscription_lifecycle_schedules.py",
  "dotmac_starter_mt:packages/dotmac-kernel/src/dotmac_kernel/messaging/outbox.py",
  "dotmac_starter_mt:packages/dotmac-kernel/src/dotmac_kernel/messaging/relay.py",
  "dotmac_starter_mt:packages/dotmac-kernel/src/dotmac_kernel/messaging/platform_relay.py",
  "dotmac_starter_mt:packages/dotmac-kernel/src/dotmac_kernel/messaging/models.py",
]
preserved_tests = [
  "dotmac_sub:tests/test_durable_timers.py",
  "dotmac_starter_mt:tests/test_outbox_relay.py",
  "dotmac_starter_mt:tests/test_platform_outbox_relay.py",
  "dotmac_starter_mt:tests/test_outbox_dispatcher_grants.py",
  "dotmac_starter_mt:tests/test_platform_outbox_dispatcher_grants.py",
]
contract_consumers = []
candidate_consumers = ["dotmac_sub", "dotmac_vendor_control_plane", "dotmac_crm"]
composition_boundary = "ADR-0024: each adopter pins the package, selects its own plane set, runs the dt lineage in its own database and owns its own timer, rejection-evidence and outbox rows. Applications exchange only versioned observations through Integrator; they never share mod_timers tables or read another application's database. ADR-0028: tenant tables carry tenant_id NOT NULL with FORCE RLS and platform tables carry no tenant column and are fully revoked from app_user; the planes share behavior but never a row or foreign key. dotmac_kernel.outbox_relay.v1 is the sole claim/lease/retry/dead-letter owner. Output routing codes are declared by their consuming module and resolved from the installed manifest set before scheduling; product command names and business policy remain in the adopter."
inventory_evidence = [
  "docs/inventories/durable-timers-sources.md",
  "docs/inventories/p11-adoption-status.md",
  "docs/adr/0024-apps-compose-by-synchronizing-data.md",
]
first_cutover = "dotmac_sub tenant plane, financial.walled_account_healing first and billing.contracts second. The package's ten PostgreSQL proofs and their sensitivity companions passed on the Dotmac Observer at exact merged candidate revision 4acb0e9716275fa6e9f2be99902bd2a6f7aa96b0 on 2026-08-18. Kernel 0.1.0a72 and dotmac-durable-timers 0.1.0a1 were then published, installed from Forgejo, registered and tagged from protected-main merge f7d69f7d3db6a36dcccaa847dff7a37e9c3cd685 on 2026-08-19; cutover begins only after Sub pins those exact artifacts. Each family shadows the module output against the existing timer owner, proves stale-generation rejection and replay safety, then switches the sole schedule/cancel/accept writer before the local durable-timer copy and due scanner are retired."
shadow_and_drift = "For each Sub timer family, schedule through the module in shadow with the same explicit identity, due instant and source version while leaving the legacy owner authoritative. Compare generation, supersession, cancellation and emitted output for every transition; record divergence without allowing the shadow to cause the consumer effect. Exercise relay lease recovery and poison isolation against PostgreSQL. Cut over only after no divergence remains, then make the module acceptance verdict the gate immediately before the local consumer effect. A two-directional caller and import ratchet prevents either copy returning."
local_copy_retirement = "dotmac_sub retires direct DurableTimer imports and queries, current_timer, schedule_timer, cancel_timer, fire_due_timers, the durable_timers table, app/tasks/durable_timers.py and the durable_timer_dispatch_runner after both named first families cut over. Product business vocabulary, command dispatch and source-state validation stay in Sub; only timing mechanics leave. The collections.case_action_due output moves with its collections owner or is removed. The lifecycle schedule command ledger remains a separate decision: only its duplicated timing claim/lease/retry behavior is eligible, not its commands. ERP has no qualifying durable timer implementation and gains no parallel local copy."
next_action = "Pin dotmac-kernel==0.1.0a72 and dotmac-durable-timers==0.1.0a1 in Sub, compose the tenant plane, and begin financial.walled_account_healing shadow adoption. Preserve the local timer as authority until the recorded parity gate passes; then cut over the sole schedule/cancel/accept writer before retiring the local copy."
