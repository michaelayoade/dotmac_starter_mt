# Changelog

## 0.1.0a1 — 2026-08-21

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `bfc112fc` by release run
`32481951010`. Publication is supply-chain evidence only; it composes no product
and moves no authority.

- Adds the tenant-only `mod_nettop` lineage (`nt` revision prefix).
- Owns declared/observed network links, forwarding resolution, paths,
  reachability and rebuildable topology projections.
- Exposes `versions_dir()` as the public installed-lineage locator.
