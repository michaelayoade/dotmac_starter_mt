# Changelog

## 0.1.0a1 — 2026-08-21

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `8b8e3d26` by release run
`32486476087`. Publication is supply-chain evidence only; it composes no product
and moves no authority.

- Records the greenfield-after-inventory source ruling and ADR-0055 boundary.
- Freezes typed collection, privacy, registry, projection, retention and drift
  contracts without an adopter website or provider branch.
- Adds the tenant-only `mod_webanalytics` lineage with forced RLS, composite
  tenant uniqueness and database-enforced append-only observation evidence.
- Adds privacy-minimised event/page-view ingestion, replay/conflict evidence,
  property-scoped pseudonyms, versioned classifier evidence, deterministic
  session/aggregate/funnel rebuilding, retention/privacy deletion and drift
  detection/repair.
