# Changelog

## 0.1.0a1 — 2026-08-21

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `bfc112fc` by release run
`32482437611`. Publication is supply-chain evidence only; it composes no product
and moves no authority.

- Adds the tenant-only `mod_fiber` lineage (`fp` revision prefix).
- Owns outside-plant structures, cables, strands, splices, terminations,
  continuity, field evidence and controlled as-built changes.
- Exposes `versions_dir()` as the public installed-lineage locator.
