# Changelog

## 0.1.0a1 — 2026-08-21

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `bfc112fc` by release run
`32481780511`. Publication is supply-chain evidence only; it composes no product
and moves no authority.

- Adds the tenant-only `mod_netobs` lineage (`no` revision prefix).
- Owns typed network observations, measurements, availability facts, alert
  evidence and rebuildable health projections.
- Exposes `versions_dir()` as the public installed-lineage locator.
