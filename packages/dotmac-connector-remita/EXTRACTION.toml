schema_version = 1
package = "dotmac-connector-remita"
classification = "stateless-protocol-adapter"
status = "audit-complete"
source_mode = "product-first"

owner = "Remita transport: authenticated RRR status polling, provider response parsing and provider-neutral status observation translation."

contract = "Implement dotmac-integration SPI >=1.3,<2.0 for connector key remita and capability payments.reference.status.observation.v1 in POLL mode only. Carry provider status, message, amount, reference, transaction and date evidence verbatim. Own no RRR lifecycle, biller policy, payment-state mapping, source linkage, tenant, product identity, ledger, journal, checkpoint, retry engine, session or product database."

source_repositories = ["dotmac_erp", "dotmac_sub", "dotmac_starter_mt"]

source_revisions = [
  "dotmac_sub:457882482cc938bfe70f48ca2fb282dab98beee6",
  "dotmac_erp:0dc07e4b6dd36260c9510a7115dbdc656e2a19a5",
]

source_paths = [
  "dotmac_erp@c656bb9070b7f35659f1968e44823e4727b309b9:app/services/remita/client.py",
  "dotmac_erp@c656bb9070b7f35659f1968e44823e4727b309b9:app/services/remita/rrr_service.py",
  "dotmac_starter_mt@bfc112fc8eac58e5ce279c1d0a5868c1f156c0e3:docs/inventories/payment-connector-extraction-dossier.md",
]

preserved_tests = [
  "dotmac_starter_mt:tests/unit/test_remita_connector.py",
  "dotmac_starter_mt:tests/architecture/test_remita_connector_boundary.py",
]

contract_consumers = []
candidate_consumers = ["dotmac_integrator", "dotmac_erp"]

inventory_evidence = [
  "docs/inventories/payment-connector-sources.md",
  "docs/inventories/payment-connector-extraction-dossier.md",
  "docs/superpowers/plans/2026-08-21-integration-ecosystem-programme.md",
  "docs/adr/0024-apps-compose-by-synchronizing-data.md",
]

first_cutover = "ERP RRR status observations through dotmac_integrator. Run the Integrator poll beside ERP's status refresh and compare the provider reference, status, amount, transaction id and payment date while ERP remains the only writer of RRR and accounting state."

shadow_and_drift = "For every configured reference compare authenticated provider responses and the ERP-recorded source evidence. Require stable event identity for an unchanged response, one new observation for each changed provider fact, and no unexplained field drift. Provider status codes are compared verbatim because ERP's current status comments disagree with the provider's current contract."

local_copy_retirement = "Delete ERP's Remita status HTTP transport, provider credential reader and polling schedule after cutover, lowering the external-connector ratchet in the same change. ERP keeps RRR intent and lifecycle, biller/service selection, source linkage, payment decisions and every accounting consequence."

next_action = "Validate on Observer and GitHub CI. After dotmac-integration 0.1.0a11 and this package are published, exact-pin them in dotmac_integrator and execute the ERP shadow/cutover. RRR issuance remains blocked on a durable request-response seam and is not disguised as fire-and-forget delivery."
[[product_writers]]
product = "dotmac_erp"
writer_state = "qualifying_source"
retirement_required = true
revision = "0dc07e4b6dd36260c9510a7115dbdc656e2a19a5"
evidence_paths = ["app/services/remita/client.py", "app/services/remita/rrr_service.py"]
[[product_writers]]
product = "dotmac_sub"
writer_state = "inventory_only"
retirement_required = false
revision = "457882482cc938bfe70f48ca2fb282dab98beee6"
evidence_paths = ["docs/SOT_RELATIONSHIP_MAP.md", "pyproject.toml"]
