# Changelog

## 0.1.0a1

- Add the first stateless Mailcow `email.lifecycle.v1` provisioning connector.
- Add held-secret HTTPS transport, exact manifest/schema declaration, typed
  plan/apply/observe/cancel behavior, public-only DKIM evidence and fail-closed
  safety gates for unproven identity and secret-write operations.
