# Changelog

## 0.1.0a1

- Add a stateless, mutually authenticated HTTPS client for the exact three
  managed-host lifecycle contracts.
- Add closed typed capability and plan/apply/observe/cancel routes, exact schema
  validation and stable ambiguity classification.
- Add named catalogue-trust, immutable-backup and health-evidence activation
  gates and freeze the separately required target-agent protocol.
