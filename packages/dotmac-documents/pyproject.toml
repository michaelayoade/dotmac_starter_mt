[tool.poetry]
name = "dotmac-documents"
version = "0.1.0a1"
description = "Controlled document identity, immutable versions and document-scoped lifecycle"
authors = ["DotMac"]
readme = "README.md"
packages = [{ include = "dotmac_documents", from = "src" }]
include = [
  { path = "src/dotmac_documents/py.typed", format = ["sdist", "wheel"] },
  { path = "src/dotmac_documents/migrations/**/*", format = ["sdist", "wheel"] },
]

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
dotmac-kernel = ">=0.1.0a85"
sqlalchemy = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
