# Changelog

## 0.1.0a1 — Unreleased

- Add the tenant-only `mod_analytics` lineage.
- Add versioned metric declarations, immutable receipts and aggregate
  observations, kernel-owned replay/conflict handling and the rebuildable point
  projection.
- Add exact latest, history, explicit-period comparison, digest and rebuild
  services without product, provider, clock or transaction ownership.
