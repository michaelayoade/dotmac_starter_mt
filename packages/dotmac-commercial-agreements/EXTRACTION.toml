schema_version = 1
package = "dotmac-commercial-agreements"
classification = "optional-module"
status = "adopted"
source_mode = "product-first"
owner = "The durable commercial agreement between the platform operator and a counterparty: its identity and reference, its immutable accepted commercial snapshot, its evidence-bound lifecycle, its amendments and supersession, and the append-only record of every transition"
contract = "Open a draft against an opaque counterparty reference with promised lines naming a product, an opaque release/offer reference, a capability code and terms frozen as exact text; validate every promised code against the declaring product through a caller-supplied catalogue port; freeze an immutable accepted snapshot and its SHA-256 digest at propose; move draft -> proposed -> approved -> active -> suspended -> terminated | expired, plus reject, cancel and amend; require ApprovalEvidence whose content_digest equals the frozen digest for both approve and activate, and separate ActivationEvidence naming a satisfied rule with a reference; refuse a stale expected status or record version; make every command idempotent through the kernel's one at-most-once owner; append one immutable history row per transition carrying its evidence document; and emit one versioned fact per transition on the platform outbox. NOT product or release definitions, entitlement balances or allocations, licences, deployment state, invoices or collections decisions, approval workflow internals, counterparty master data, or stored bytes."
source_repositories = [
  "dotmac_vendor_control_plane",
  # Inventoried and found EMPTY, which is why they are listed. ERP's
  # `templates/procurement/contracts` is supplier procurement documents — a
  # shared word and nothing else — and Sub has no vendor<->operator agreement
  # at all; what it sells is ISP service to subscribers, a different subject
  # with a different owner (ruling A2(a)). Recorded per
  # docs/inventories/vendor-cp-gap-sources.md § 10, so the absence is evidence
  # rather than an untaken search.
  "dotmac_erp",
  "dotmac_sub",
  "dotmac_starter_mt",
]
source_paths = [
  "dotmac_vendor_control_plane:src/vendor_cp/contracts/service.py",
  "dotmac_vendor_control_plane:src/vendor_cp/contracts/models.py",
  "dotmac_vendor_control_plane:src/vendor_cp/contracts/schemas.py",
  "dotmac_vendor_control_plane:src/vendor_cp/contracts/router.py",
  "dotmac_vendor_control_plane:alembic/versions/v004_contracts.py",
  "dotmac_vendor_control_plane:docs/design/contract-service.md",
  "dotmac_vendor_control_plane:docs/adr/0003-product-qualified-commercial-identity.md",
  "dotmac_starter_mt:packages/dotmac-commercial-agreements/src/dotmac_commercial_agreements",
]
preserved_tests = [
  "dotmac_vendor_control_plane:tests/unit/test_contracts.py",
  "dotmac_starter_mt:tests/unit/test_commercial_agreements_lifecycle.py",
  "dotmac_starter_mt:tests/unit/test_commercial_agreements_evidence.py",
  "dotmac_starter_mt:tests/architecture/test_commercial_agreements_module.py",
  "dotmac_starter_mt:tests/test_commercial_agreements_platform_isolation.py",
]
contract_consumers = ["dotmac_vendor_control_plane"]
candidate_consumers = []
adoption_evidence = [
  "dotmac_vendor_control_plane:main@af9fcf6d3fbd259fbef6b589d37b39d548f7ba8e",
  "dotmac_vendor_control_plane:production-deploy#32485479666",
  "ghcr.io/michaelayoade/dotmac_vendor_control_plane@sha256:45715e425dc248d85fe374fa5d347087328a445cf7ead1f8abc29f05f0117b0d",
  "dotmac_vendor_control_plane:migration:v015_agreements_authority",
  "dotmac_vendor_control_plane:live-schema:mod_agreements",
]
composition_boundary = "ADR-0024: the vendor control plane installs its own cg lineage and owns its own agreement rows; no application shares a mod_agreements schema, an agreement row or a history row. ADR-0057 § 8: the module imports no sibling. dotmac-approvals supplies ApprovalEvidence as a VALUE through the assembly and dotmac-entitlement-allocation consumes agreement.activated.v1 the same way — neither is imported, and the module never executes another owner's consequence (ADR-0026 § 6). ADR-0006 D1: no foreign key leaves mod_agreements; counterparty_ref, release_ref, offer_ref and approval_decision_ref are opaque and unconstrained, so an executed agreement outlives a superseded release, a retired approval policy and a merged counterparty record. ADR-0019 § 1 and ruling A3: the counterparty reference is opaque and this module defines no counterparty master. ADR-0023 / ADR-0057 § 7: platform plane only, declared from the one consumer that exists today; tables is empty and no tenant plane is declared for later use."
inventory_evidence = [
  "docs/inventories/vendor-cp-gap-sources.md",
  "docs/adr/0057-the-vendor-control-plane-composes-existing-owners.md",
]
first_cutover = "Completed GREENFIELD on 2026-08-21 in dotmac_vendor_control_plane, not by row migration. The designated production target was measured empty, so v015_agreements_authority rechecks the premise under ACCESS EXCLUSIVE and refuses a populated estate rather than synthesizing contracts, hashes or history. Vendor exact-pins dotmac-commercial-agreements 0.1.0a1, composes the platform-only manifest and cg_0001_agreements lineage, and retains one typed adapter that resolves immutable offers, supplies the product capability catalogue and converts the authoritative Approvals request into content-bound evidence. Verified directly against the production database at 2026-08-21T14:17:32Z: mod_agreements live, public.contracts and public.contract_lines ABSENT, and app_user holding zero privileges on any mod_* schema."
shadow_and_drift = "No shadow phase is owed, and the reason is measurable rather than convenient. The capability exists in exactly one product (Knowledge `vendor-cp-capability-gaps-and-composable-plan`, Tier B: contracts 972 LOC / 2 tables), so there is no second writer to reconcile against and no parallel evaluator to compare. What IS owed at cutover is a data-bearing migration of the existing vendor_cp.contracts and contract_lines rows, because those rows are real: preserve every content_hash unchanged (an agreement whose digest moves invalidates its own approval), map pending_approval -> proposed, synthesise one AgreementEvent per historical transition from the platform audit log rather than inventing a clean history, and prove the pre- and post-cutover accepted snapshots digest identically. A populated legacy table encountered at cutover must fail the migration, not be silently skipped."
local_copy_retirement = "Complete for cutover 1 on 2026-08-21: src/vendor_cp/contracts/ service and models, and public.contracts / public.contract_lines are gone, verified absent on the production database. Original audit text follows. Owed by dotmac_vendor_control_plane at cutover: src/vendor_cp/contracts/{models,service,schemas,router,feature}.py, the contracts and contract_lines tables, and the ContractService call sites in the allocations consumer. The vendor keeps its own routes, its console screens and its vendor_accounts binding — a thin adapter that translates its account id into this module's opaque counterparty_ref. No product may keep a compatibility contracts table or a parallel lifecycle evaluator."
next_action = "Hold at `adopted`. `reuse-proven` requires a SECOND real control plane completing its own cutover; there is no second candidate today. Keep the Vendor authority and zero-local-writer ratchets green. Do not add a delivery or acknowledgement concern here — dotmac-licensing owns that lifecycle and ADR-0010 moves its transport half to the Integrator."
