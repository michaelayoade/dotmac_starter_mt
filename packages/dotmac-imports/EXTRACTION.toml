schema_version = 1
package = "dotmac-imports"
classification = "optional-module"
status = "audit-complete"
source_mode = "product-first"
owner = "The durable record of a bulk import: the run, its rows, and what became of each one"
contract = "Record an operator-initiated import of one stored document; verify the raw bytes against the recorded SHA-256 at both validation and apply; validate without holding anything that could mutate; promote one validated run into one apply run; advance one FOR UPDATE-locked, caller-transaction-owned chunk from the durable checkpoint per call; commit each domain effect with its minimised row outcome; resume a running run and make completed re-delivery a no-op. Persist only a row fingerprint, bounded typed safe errors and an opaque result — never mapped row values or raw exception text. Decode CSV strictly and resolve source headings onto a domain-declared field set. NOT the field vocabulary, business validation, duplicate policy, the domain mutation, any reversal of it, the stored bytes, or export."
source_repositories = [
  "dotmac_erp",
  "dotmac_sub",
  "dotmac_crm",
  "dotmac_academy_app",
  "dotmac_vendor_control_plane",
  "dotmac_starter_mt",
]
source_paths = [
  "dotmac_sub:app/services/import_runs.py",
  "dotmac_sub:app/models/imports.py",
  "dotmac_sub:alembic/versions/180_import_runs.py",
  "dotmac_sub:app/services/web_system_import_wizard.py",
  "dotmac_sub:app/imports/loader.py",
  "dotmac_erp:app/services/finance/import_export/base.py",
  "dotmac_erp:app/services/finance/import_export/import_service.py",
  "dotmac_erp:app/services/imports/formats.py",
  "dotmac_crm:app/imports/loader.py",
  "dotmac_starter_mt:packages/dotmac-imports/src/dotmac_imports",
]
preserved_tests = [
  "dotmac_sub:tests/test_import_runs.py",
  "dotmac_sub:tests/test_system_import_wizard_service.py",
  "dotmac_sub:tests/test_import_rollback_money_safety.py",
  "dotmac_erp:tests/ifrs/import_export/test_base_importer.py",
  "dotmac_erp:tests/ifrs/import_export/test_base_importer_spreadsheets.py",
  "dotmac_erp:tests/ifrs/import_export/test_validation_rules.py",
  "dotmac_erp:tests/architecture/test_importer_dry_run_writes_nothing.py",
  "dotmac_starter_mt:tests/unit/test_imports.py",
  "dotmac_starter_mt:tests/architecture/test_imports_module.py",
  "dotmac_starter_mt:tests/test_imports_isolation.py",
]
contract_consumers = []
candidate_consumers = ["dotmac_erp", "dotmac_sub", "dotmac_crm"]
composition_boundary = "ADR-0024: each adopter installs its own im lineage and owns its own run ledger. Applications share the package contract, never a mod_imports schema or a run row. An import performed in one application is that application's fact; another app learns of its results through the owning app's versioned API/webhook, never by reading its ledger. The domain's validator and applier are product code supplied at the call site, never product/provider switches inside the module."
inventory_evidence = [
  "docs/inventories/imports-sources.md",
  "docs/adr/0025-imports-own-the-run-not-what-a-row-means.md",
]
first_cutover = "dotmac_erp is cutover 1. Product-first requires a qualifying source product to retire its local owner, and ERP is the half with no durable record at all — its adoption is a capability gain rather than a relocation, which makes the parity evidence unusually clear (before: an in-memory ImportResult; after: a queryable run). It is gated behind the same E8 Organization-to-Tenant and composed-lineage decision that gates dotmac-files, because tenant_id NOT NULL cannot be satisfied before it. dotmac_sub is cutover 2, retiring app/services/import_runs.py and moving its two tables into the im lineage while web_system_import_wizard's ENTITY_CONFIG stays in Sub as the domain half. dotmac_crm is cutover 3 and the cheapest: delete the forked app/imports/loader.py. Neither dotmac_academy_app nor dotmac_vendor_control_plane is a candidate — Academy's content_import is markdown authoring over a filesystem tree and Vendor CP has no import capability at all."
shadow_and_drift = "ERP's cutover runs each entity importer's existing in-memory path and the module-backed path over the same document and compares row-for-row: same accepted count, same rejected rows with equivalent typed safe reasons, same resulting entity ids. Because ERP has no prior ledger there is no historical record to backfill and no drift class from one — the comparison is between two live paths, and the module's ledger becomes the first durable one. Sub's cutover is the opposite shape: its ledger already exists, so every import_runs / import_run_rows row is compared against the module row created from the same input (kind, status, counts, per-line status and classified error), the payment provenance columns are verified to have moved to the domain side with no row losing its link, and the inline input_text is verified against the dotmac-files object's SHA-256 before the column is dropped. Imported row values are not copied into the new ledger. Both cutovers run the new path in dry-run only until the row-for-row comparison is clean."
local_copy_retirement = "ERP must reduce app/services/finance/import_export/base.py to a domain FieldSet plus its 23 validators and appliers, with decoding, mapping, preview and the run loop coming from the pinned dotmac-imports release; a two-directional import/caller ratchet reaches zero before BaseImporter is deleted. Its COLUMN_ALIASES, VALID_ACCOUNT_TYPES, VALID_CURRENCY_CODES and vendor-format detectors stay in ERP as finance vocabulary and must not migrate into the module. Sub must delete app/services/import_runs.py and its import_runs / import_run_rows tables after the module lineage is composed, keeping ENTITY_CONFIG and _persist_row as the domain half; PaymentImportBatchReversal stays in billing and is never reproduced in the module. CRM deletes app/imports/loader.py outright. No permanent mirrored ledger, second run table, or parallel dry-run implementation is allowed."
next_action = "Keep the module audit-complete and unreleased until a real first adopter is ready. Finish Sub's reference kernel-lineage production gate, then ERP's E8 Organization-to-Tenant/composed-lineage decision and dotmac-files adoption. Cut ERP's customer-master CSV importer over first behind row-for-row dry-run comparison; publish the exact kernel/imports releases just in time for that cutover. Port ERP's XLSX/XLSM reader as an openpyxl extra and legacy XLS as an xlrd extra with their source parity tests in the adopter change, not as untested formats ahead of a consumer. Sub and CRM follow. PDF remains a separate decoder-adapter candidate awaiting real source evidence. Export remains unowned and undossiered; the CSV formula-injection exposure the audit found on the export side is recorded in the inventory and is NOT addressed by this module."
