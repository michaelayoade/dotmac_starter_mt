# Compatibility — dotmac-remote-access

Version `0.1.0a1` requires `dotmac-kernel>=0.1.0a88`, which allocates
`mod_remoteaccess` and prefix `ra`. The package is tenant-only and is not composed by
the Starter reference assembly.
