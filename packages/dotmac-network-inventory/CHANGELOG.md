# Changelog

## 0.1.0a1 — 2026-08-21

Published, installed back from the private index, conformance-checked and
tagged from exact protected-main revision `bfc112fc` by release run
`32481619155`. Publication is supply-chain evidence only; it composes no product
and moves no authority.

- Adds the tenant-only `mod_netinv` lineage (`ni` revision prefix).
- Owns managed sites, nodes, interfaces, ports, VLANs, roles/capabilities,
  admission, archive and configuration snapshot metadata.
- Exposes `versions_dir()` as the public installed-lineage locator.
