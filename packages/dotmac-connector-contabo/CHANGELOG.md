# Changelog

## 0.1.0a1

- Add the stateless Contabo PROVISION plugin for exact managed firewall
  lifecycle, with a bounded real HTTPS/OAuth transport.
- Add deterministic create discovery and ambiguous mutation classification.
- Record named exact-contract activation gates for instance, network, volume,
  and authoritative DNS rather than approximating unsupported semantics.
