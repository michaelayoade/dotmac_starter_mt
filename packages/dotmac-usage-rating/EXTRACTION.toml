schema_version = 1
package = "dotmac-usage-rating"
classification = "optional-module"
status = "audit-complete"
source_mode = "product-first"
owner = "Effective usage rating and immutable pre-tax obligations"
contract = "Own versioned/effective meter rating rules and immutable rated usage obligations. Usage and service identities are opaque references. NOT usage collection, catalogue offers, subscription contracts, invoicing, tax, payment, revenue recognition or general-ledger posting."
source_repositories = ["dotmac_sub", "dotmac_erp", "dotmac_starter_mt"]
source_paths = [
  "dotmac_sub:app/models/usage_rating.py",
  "dotmac_sub:app/services/usage_rating.py",
  "dotmac_sub:app/services/billing_usage.py",
  "dotmac_erp:app/models/accounts/invoice.py",
]
preserved_tests = [
  "dotmac_sub:tests/test_usage_rating.py",
  "dotmac_sub:tests/test_usage_billing.py",
  "dotmac_starter_mt:tests/unit/test_usage_rating_service.py",
  "dotmac_starter_mt:tests/architecture/test_usage_rating_module.py",
  "dotmac_starter_mt:tests/test_usage_rating_isolation.py",
]
contract_consumers = []
candidate_consumers = ["dotmac_sub"]
composition_boundary = "Adopters compose ur locally and submit opaque normalized-usage references and quantities. Billing consumes immutable pre-tax obligations through a typed port; no sibling module import or foreign database read is permitted."
inventory_evidence = ["docs/inventories/isp-essential-domain-sources.md"]
first_cutover = "Sub remains usage-rating owner until rule and obligation backfill plus pre-tax amount shadow comparison complete."
shadow_and_drift = "Compare effective rule, meter/unit, quantity, unit price, currency, net amount and rating time. Invoice/tax/GL state is excluded."
local_copy_retirement = "After cutover, reduce Sub rating-rule and pre-tax-obligation writers to zero while Billing consumes the new typed obligation contract."
next_action = "Compose in Sub and run rule/obligation shadow rating before switching the sole writer."
