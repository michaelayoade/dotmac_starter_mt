schema_version = 1
package = "dotmac-referrals"
classification = "optional-module"
status = "audit-complete"
source_mode = "product-first"
owner = "Tenant referral programmes, invitation/code identity, attribution, qualification and immutable conversion/reward evidence"
contract = "Create and version referral programmes; issue a stable invitation/code to an opaque referrer; attribute a referred party/prospect without resolving identity from unverified contact data; preserve conversion and reward-request evidence; enforce idempotent capture and terminal rejection/expiry; and emit typed outbox facts. NOT Party/Customer/Lead creation, billing credits, reward payment, campaigns, delivery transport or provider I/O."
source_repositories = ["dotmac_sub", "dotmac_crm", "dotmac_erp", "dotmac_starter_mt", "dotmac_integrator", "dotmac_vendor_control_plane", "dotmac_workspace", "dotmac_academy_app"]
source_paths = ["dotmac_sub:app/models/referral_native.py", "dotmac_sub:app/services/referrals.py", "dotmac_sub:app/services/referral_account_conversion.py", "dotmac_crm:app/models/crm/referral.py", "dotmac_crm:app/services/crm/referrals.py"]
preserved_tests = ["dotmac_sub:tests/test_referrals_native.py", "dotmac_sub:tests/architecture/test_referrals_program_boundary.py", "dotmac_sub:tests/test_referral_account_conversion.py", "dotmac_starter_mt:tests/unit/test_referrals.py", "dotmac_starter_mt:tests/test_sub_vnext_modules_isolation.py"]
contract_consumers = []
candidate_consumers = ["dotmac_sub"]
adoption_evidence = []
composition_boundary = "Party, Customers, Sales/Leads and Billing remain named owners. The adopter supplies opaque party/customer/lead references through typed ports and reacts to outbox facts in its own transaction boundary; the module has no sibling import, product FK, cross-lineage FK, contact resolver, credit writer or transport."
inventory_evidence = ["docs/inventories/sub-vnext-parity-sources.md", "docs/inventories/sub-vnext-parity-dispositions.toml", "docs/adr/0034-sub-vnext-parity-capabilities-have-narrow-independent-owners.md"]
first_cutover = "Sub is cutover 1. Backfill programme/code/referral rows with exact identities and evidence, quarantine ambiguous contacts, shadow complete referral fingerprints and refusal outcomes, seal Sub's local writer, then lower and ultimately remove CRM/Sub referral-writer ratchets."
shadow_and_drift = "Compare every programme version, active invitation/code, attribution, state, conversion reference and reward-request reference at one immutable watermark. Zero unexplained rows and byte-identical aggregate fingerprints are required before the writer switch."
local_copy_retirement = "CRM's direct Referral/ReferralCode service and tables retire after Sub/module authority is sealed; Sub's referral_native tables and mutation adapters retire after its own module cutover. Billing, Party and Lead collaborators remain because they own different facts."
next_action = "Implement the tenant-only rf lineage from Sub behavior with RLS/FORCE, typed ports, outbox facts and parity canaries; do not release or cut over until Observer and Sub adoption gates pass."
