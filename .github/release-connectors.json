{
  "$comment": [
    "The CLOSED set of CONNECTOR PLUGIN distributions this repository may",
    "publish, and the conformance every entry must have passed first.",
    "`scripts/release_connector.py resolve` gates on it and fails closed, so",
    "listing a connector is a reviewed diff rather than a dispatch-time string —",
    "the doctrine of `.github/release-modules.json` and",
    "`.github/release-adapters.json`, applied to a third shape.",
    "",
    "## Why a THIRD file",
    "",
    "A connector is neither of the two shapes already governed.",
    "",
    "It is not a MODULE: it owns no `mod_*` schema, no migration lineage and no",
    "`MIGRATION_OWNER_LEDGER` allocation. Its state lives in the control plane's",
    "`mod_intg`, which `dotmac-integration` owns and a connector never writes",
    "directly. The module lane would ask it for a `db_schema` to compare against",
    "a manifest and a `kernel_floor` naming the release that allocated it, and",
    "it has neither.",
    "",
    "It is not a STATELESS PROTOCOL ADAPTER either, and this is the distinction",
    "that earns the file. An adapter is a library a product CALLS: it is",
    "verified by its public surface, and nothing else in the fleet has an",
    "opinion about it. A connector is DISCOVERED and EXECUTED by a control",
    "plane it never imports the caller of — it registers in the",
    "`dotmac_integration.connectors` entry-point group, targets a versioned SPI,",
    "and is loaded at boot by `dotmac_integration.discovery.discover`, which is",
    "FAIL-CLOSED AS A SET: one malformed connector refuses the whole registry",
    "rather than silently offering the rest. So a connector's release gate has",
    "to answer a question neither other lane asks — 'does this distribution",
    "satisfy the SPI contract?' — and the answer is a function call, not a",
    "review comment: `dotmac_integration.conformance`.",
    "",
    "Making the adapter lane's fields optional was rejected for the reason that",
    "lane already records about the module lane: optionality is not scoped to",
    "the package that needs it. Once `spi_range` and `connector_key` may be",
    "absent, a connector whose entry-point registration was dropped in a bad",
    "merge stops being refused and starts being treated as an adapter, its",
    "conformance proof skipped rather than failed.",
    "",
    "## A release PROFILE, not a new extraction classification",
    "",
    "`connector-plugin` is the name of this RELEASE PROFILE and of the",
    "architectural role. It is deliberately NOT an `EXTRACTION.toml`",
    "classification, and a connector's dossier says",
    "`classification = \"stateless-protocol-adapter\"` like any other",
    "distribution a product does not install. Adding a fourth classification",
    "would require amending ADR-0006 and the global validator for no gain — the",
    "four properties that classification governs (no ModuleManifest, no",
    "lineage, no ledger allocation, no persistence import) are exactly the four",
    "a connector has, and inventing a synonym for them would put the same",
    "package in two vocabularies at once.",
    "",
    "The consequence has to be stated plainly, because it is the one thing a",
    "reader could get wrong: THE CLASSIFICATION NO LONGER SEPARATES THIS LANE",
    "FROM THE ADAPTER LANE. Both accept `stateless-protocol-adapter`, so the",
    "classification check is a floor both lanes share rather than the thing",
    "that distinguishes them. What distinguishes this lane is the set of",
    "STRICTER requirements below — exactly one connector entry point, a",
    "published Integration floor, connector-key consistency, installed-wheel",
    "SPI conformance, and no persistence, secret material or private",
    "retry/checkpoint engine. Those are checks the adapter lane does not make,",
    "and they are why a connector cannot simply be released through it.",
    "`tests/architecture/test_connector_release_policy.py` proves the point the",
    "hard way: `dotmac-auth-oidc` carries the identical classification and is",
    "still refused by this gate.",
    "",
    "## What earns an entry",
    "",
    "Five things, and every one of them is checked rather than asserted in",
    "review. `scripts/release_connector.py` is where each is enforced:",
    "",
    "  1. CLASSIFICATION and LOCATION. `EXTRACTION.toml` says",
    "     `stateless-protocol-adapter`, read from the package and never trusted",
    "     from this file — that is what stops this lane becoming a way to",
    "     publish a stateful module while skipping the namespace, lineage and",
    "     dual-plane gates the module lane performs. And `package_dir` must",
    "     start with `packages/dotmac-connector-`: first-party connectors live",
    "     under Starter `packages/` as independently built, tested, versioned",
    "     and published distributions, and the prefix is ENFORCED rather than",
    "     conventional so a connector cannot be released from somewhere the",
    "     naming does not announce it.",
    "  2. DISCOVERY. The package registers exactly one entry point in the",
    "     `dotmac_integration.connectors` group, and the `connector_key` this",
    "     entry declares is the one its manifest declares. A key mismatch is",
    "     invisible until two connectors collide in a live registry.",
    "  3. CONFORMANCE. `assert_plugin_conforms` passes against the INSTALLED",
    "     bytes — not the source tree, because only one of the two gets",
    "     published. It subsumes `assert_connector_conforms`, so metadata and",
    "     executability are proved together: a distribution that declares a",
    "     capability it cannot hand back a handler for passes every metadata",
    "     check and fails at the first dispatch.",
    "  4. AN INSTALLABLE FLOOR. `integration_floor` names the earliest",
    "     `dotmac-integration` release whose SPI admits this connector — and it",
    "     must be a PUBLISHED one. `release-modules.json` already states the",
    "     rule for kernels: 'A floor naming an unpublished version cannot be",
    "     resolved by an installer, so it is not a floor at all.' Here it is",
    "     enforced rather than only written down: `resolve` refuses an",
    "     `integration_floor` with no release tag. This bites on every SPI bump:",
    "     the current connector may floor at registry-verified integration a10,",
    "     while a merely declared future release remains inadmissible.",
    "  5. NO SECRET SHAPE, no persistence, and no private retry/checkpoint",
    "     engine. A connector holds a reference to credential material (ADR-0024",
    "     section 7); it never holds the value, it declares no tables, and it does",
    "     not carry its own delivery-retry or feed-checkpoint machinery — that is",
    "     precisely the surface `docs/inventories/external-connector-sources.md`",
    "     is ratcheting OUT of products and into the control plane. A connector",
    "     rebuilding it locally would move the duplication rather than retire it.",
    "",
    "## What an entry does NOT prove",
    "",
    "Exactly what a module allowlist row does not prove, restated because a",
    "connector is the shape most likely to be read as an endorsement of a",
    "provider integration:",
    "",
    "  * NOT that a version was published. The row says the workflow MAY",
    "    publish. `docs/inventories/declared-publication-baseline.json` is where",
    "    publication is tracked, and the two are deliberately separate files",
    "    because they answer different questions.",
    "  * NOT that anything adopted it. Adoption is `EXTRACTION.toml`'s",
    "    `contract_consumers`, which counts real cutovers, and a release moves",
    "    neither it nor `status`.",
    "  * NOT that the provider works. Conformance is proved against the SPI with",
    "    no network and no credentials — deliberately, so an author's first",
    "    experience of the SPI is not a secrets problem. A connector can conform",
    "    perfectly and still be misconfigured against a live provider; that is",
    "    what the control plane's health and quarantine paths are for.",
    "",
    "## Why the first entry is here",
    "",
    "The Meta/WhatsApp ingress connector is listed with its implementation,",
    "prebuilt conformance corpus, installed-wheel SPI proof and published",
    "dotmac-integration 0.1.0a10 floor. This is the rule ADR-0026 section 8 set",
    "for release eligibility: the entry lands WITH the proof, never ahead of it.",
    "",
    "THIS OBJECT IS THE LOCK. `.github/workflows/release-connector.yml` exists",
    "and is NOT the lock — a reader must not mistake an input option for",
    "authorization. `resolve` reads this exact object before build and again",
    "after protected-environment approval.",
    "",
    "The workflow input is now an exact choice list matching this object.",
    "`tests/architecture/test_connector_release_policy.py` keeps that UI layer",
    "and the enforced policy in sync in both directions."
  ],
  "conformance": {
    "$comment": [
      "The conformance obligations every connector must satisfy, named as the",
      "callables that enforce them. These are not documentation: the guard",
      "resolves each name in the shipped kit, so a rename in",
      "`dotmac-integration` fails here instead of leaving this policy pointing",
      "at a contract that moved."
    ],
    "kit_module": "dotmac_integration.conformance",
    "spi_module": "dotmac_integration.spi",
    "discovery_module": "dotmac_integration.discovery",
    "required_assertions": [
      "assert_connector_conforms",
      "assert_plugin_conforms"
    ],
    "entry_point_group": "dotmac_integration.connectors",
    "classification": "stateless-protocol-adapter",
    "package_dir_prefix": "packages/dotmac-connector-"
  },
  "connectors": {
    "dotmac-connector-flutterwave": {
      "$comment": [
        "Flutterwave API v4 authentication, settlement ingress and paged",
        "charge reconciliation. It implements",
        "payments.settlement.observation.v1 through SPI 1.3, owns no rows",
        "and imports no product. Sub is the first shadow/cutover target;",
        "publication alone does not move financial authority.",
        "",
        "This release accepts only v4 authentication and exact declared v4",
        "hosts. It has no v3 fallback and does not invent a provider fee.",
        "",
        "It also carries payments.intent.v1 and payments.refund.v1 outbound,",
        "over the SAME declared v4 hosts. Transfers and payouts are",
        "deliberately absent: no product consumer exists, and an outbound",
        "money-movement command whose first execution is also its first",
        "review does not ship."
      ],
      "package_dir": "packages/dotmac-connector-flutterwave",
      "import_name": "dotmac_connector_flutterwave",
      "plugin_attr": "PLUGIN",
      "connector_key": "flutterwave",
      "spi_range": ">=1.4,<2.0",
      "integration_floor": "0.1.0a14",
      "tag_prefix": "dotmac-connector-flutterwave-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_flutterwave/__init__.py",
          "dotmac_connector_flutterwave/plugin.py",
          "dotmac_connector_flutterwave/outbound.py",
          "dotmac_connector_flutterwave/polling.py",
          "dotmac_connector_flutterwave/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "httpx",
          "python"
        ]
      }
    },
    "dotmac-connector-mono": {
      "$comment": [
        "Mono Financial Data v2 account-transaction polling and neutral",
        "observation translation. It implements",
        "banking.transaction.observation.v1 through SPI 1.3, owns no rows",
        "and imports no product. ERP is the first shadow/cutover target;",
        "publication alone does not move statement or reconciliation authority.",
        "",
        "The connector accepts only the declared api.withmono.com egress,",
        "persists only an opaque Integrator-owned cursor and refuses pagination",
        "that changes origin or account. Amounts remain exact provider integers",
        "in their lowest denomination; ERP decides every accounting consequence."
      ],
      "package_dir": "packages/dotmac-connector-mono",
      "import_name": "dotmac_connector_mono",
      "plugin_attr": "PLUGIN",
      "connector_key": "mono",
      "spi_range": ">=1.3,<2.0",
      "integration_floor": "0.1.0a11",
      "tag_prefix": "dotmac-connector-mono-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_mono/__init__.py",
          "dotmac_connector_mono/plugin.py",
          "dotmac_connector_mono/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "httpx",
          "python"
        ]
      }
    },
    "dotmac-connector-meta-social": {
      "$comment": [
        "Meta Social translation, authentication and Graph delivery I/O for",
        "Facebook Messenger, Instagram DMs, Facebook comments and Instagram",
        "comments. It implements messaging.receive.v1 and messaging.send.v1",
        "through SPI 1.4, owns no rows and imports no product. Sub is the",
        "first shadow/cutover target; publication alone does not make it a",
        "contract consumer.",
        "",
        "Egress is the exact two Graph hosts the delivery slice calls. Whether",
        "a reply may be sent at all stays a product decision and never reaches",
        "this wheel."
      ],
      "package_dir": "packages/dotmac-connector-meta-social",
      "import_name": "dotmac_connector_meta_social",
      "plugin_attr": "PLUGIN",
      "connector_key": "meta_social",
      "spi_range": ">=1.4,<2.0",
      "integration_floor": "0.1.0a14",
      "tag_prefix": "dotmac-connector-meta-social-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_meta_social/__init__.py",
          "dotmac_connector_meta_social/plugin.py",
          "dotmac_connector_meta_social/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "httpx",
          "python"
        ]
      }
    },
    "dotmac-connector-linkedin": {
      "$comment": [
        "LinkedIn ingress challenge, exact-byte authentication and neutral",
        "organization-social plus Lead Sync translation. It implements two",
        "observation capabilities through SPI 1.3, owns no rows, declares",
        "deny-all egress and imports no product."
      ],
      "package_dir": "packages/dotmac-connector-linkedin",
      "import_name": "dotmac_connector_linkedin",
      "plugin_attr": "PLUGIN",
      "connector_key": "linkedin",
      "spi_range": ">=1.3,<2.0",
      "integration_floor": "0.1.0a11",
      "tag_prefix": "dotmac-connector-linkedin-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_linkedin/__init__.py",
          "dotmac_connector_linkedin/plugin.py",
          "dotmac_connector_linkedin/py.typed"
        ],
        "forbidden_prefixes": ["app/", "tests/", "alembic/", "scripts/"],
        "allowed_requires": ["dotmac-integration", "python"]
      }
    },
    "dotmac-connector-paystack": {
      "$comment": [
        "Paystack authentication, settlement ingress, paged transaction",
        "reconciliation and OUTBOUND command I/O. It implements",
        "payments.settlement.observation.v1 in INGRESS and POLL, and",
        "payments.intent.v1, payments.refund.v1, payments.payout.v1 and",
        "payments.customer.v1 in DELIVERY, through SPI 1.4. It owns no rows",
        "and imports no product. Sub is the first shadow/cutover target;",
        "publication alone does not make it a contract consumer or move",
        "financial authority.",
        "",
        "The SPI floor moves to 1.4 because a multi-mode connector must map",
        "each capability to its OWN mode: without that mapping conformance",
        "calls the ingress factory for a delivery-only contract, and a",
        "settlement watcher's binding would carry the authority to move money.",
        "",
        "The outbound half decides nothing. Settlement admissibility, invoice",
        "targeting, allocation, coverage, whether a refund is warranted and",
        "every ledger consequence remain the product's. The connector performs",
        "the command it is given, classifies the answer, and reports an",
        "inconclusive one as reconciliation-required rather than retrying",
        "money."
      ],
      "package_dir": "packages/dotmac-connector-paystack",
      "import_name": "dotmac_connector_paystack",
      "plugin_attr": "PLUGIN",
      "connector_key": "paystack",
      "spi_range": ">=1.4,<2.0",
      "integration_floor": "0.1.0a14",
      "tag_prefix": "dotmac-connector-paystack-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_paystack/__init__.py",
          "dotmac_connector_paystack/delivery.py",
          "dotmac_connector_paystack/operations.py",
          "dotmac_connector_paystack/plugin.py",
          "dotmac_connector_paystack/polling.py",
          "dotmac_connector_paystack/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "httpx",
          "python"
        ]
      }
    },
    "dotmac-connector-nira": {
      "$comment": [
        "NiRA .ng registry transport over CoCCA EPP. It serves the eight",
        "registry.* DELIVERY capabilities (domain/host/contact check, create,",
        "info, renew, update, transfer) and registry.message.v1 over EPP poll,",
        "through SPI 1.4, owns no rows and imports no product. The wire shape",
        "follows the CoCCA reference module NiRA distributes to registrars; the",
        "domain lifecycle, price and retry all stay with the owning application",
        "and the engine.",
        "",
        "HELD BACK, deliberately, not a gap: this connector is listed so its",
        "absence is a decision rather than an omission, but it is NOT released.",
        "It has not been proven against a live registry — OT&E EPP login from",
        "the Integrator host returns 2202 until that host's IP is whitelisted",
        "in the CoCCA console, and its capability owner (dotmac-domains) is",
        "greenfield with no runtime consumer. It also has no third-party",
        "dependency: the EPP transport is Python stdlib (socket/ssl/struct/",
        "ElementTree), so its allowed wheel requires are dotmac-integration and",
        "python only. Release once the OT&E round-trip is green and a consumer",
        "exists."
      ],
      "package_dir": "packages/dotmac-connector-nira",
      "import_name": "dotmac_connector_nira",
      "plugin_attr": "PLUGIN",
      "connector_key": "nira",
      "spi_range": ">=1.4,<2.0",
      "integration_floor": "0.1.0a14",
      "tag_prefix": "dotmac-connector-nira-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_nira/__init__.py",
          "dotmac_connector_nira/plugin.py",
          "dotmac_connector_nira/delivery.py",
          "dotmac_connector_nira/polling.py",
          "dotmac_connector_nira/frames.py",
          "dotmac_connector_nira/epp.py",
          "dotmac_connector_nira/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "python"
        ]
      }
    },
    "dotmac-connector-remita": {
      "$comment": [
        "Remita RRR status polling and neutral observation translation. It",
        "implements payments.reference.status.observation.v1 through SPI 1.3,",
        "owns no rows and imports no product. ERP is the first shadow/cutover",
        "target; publication alone does not move RRR or accounting authority.",
        "",
        "The connector carries provider status tokens verbatim and declares",
        "only the provider's fixed demo/live hosts. RRR issuance ships as",
        "payments.reference.issuance.v1 now that dotmac-integration carries a",
        "durable command-response seam: Outcome.provider_reference and",
        "provider_status_code are persisted on delivery_attempts by",
        "ig_0012_delivery_evidence, so the minted RRR survives the call rather",
        "than being discarded by a fire-and-forget dispatch."
      ],
      "package_dir": "packages/dotmac-connector-remita",
      "import_name": "dotmac_connector_remita",
      "plugin_attr": "PLUGIN",
      "connector_key": "remita",
      "spi_range": ">=1.4,<2.0",
      "integration_floor": "0.1.0a14",
      "tag_prefix": "dotmac-connector-remita-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_remita/__init__.py",
          "dotmac_connector_remita/plugin.py",
          "dotmac_connector_remita/outbound.py",
          "dotmac_connector_remita/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "httpx",
          "python"
        ]
      }
    },
    "dotmac-connector-whatsapp": {
      "$comment": [
        "Meta WhatsApp Cloud API translation, authentication and delivery I/O.",
        "It implements messaging.receive.v1 and messaging.send.v1 through SPI",
        "1.4, owns no rows and imports no product. Sub is the first shadow/cutover",
        "target; publication alone does not make it a contract consumer.",
        "",
        "The required wheel surface is deliberately small: plugin.py is the",
        "provider adapter, __init__.py is the public/discovery surface, and",
        "py.typed keeps the handler contract typed in the Integrator assembly."
      ],
      "package_dir": "packages/dotmac-connector-whatsapp",
      "import_name": "dotmac_connector_whatsapp",
      "plugin_attr": "PLUGIN",
      "connector_key": "meta_whatsapp",
      "spi_range": ">=1.4,<2.0",
      "integration_floor": "0.1.0a14",
      "tag_prefix": "dotmac-connector-whatsapp-v",
      "wheel_contents": {
        "required": [
          "dotmac_connector_whatsapp/__init__.py",
          "dotmac_connector_whatsapp/plugin.py",
          "dotmac_connector_whatsapp/py.typed"
        ],
        "forbidden_prefixes": [
          "app/",
          "tests/",
          "alembic/",
          "scripts/"
        ],
        "allowed_requires": [
          "dotmac-integration",
          "httpx",
          "python"
        ]
      }
    }
  }
}
