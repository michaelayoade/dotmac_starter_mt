name: Verify composition contract sources
description: Refuse helper bytes that differ from the product-bound contract revision

runs:
  using: composite
  steps:
    - name: Verify trusted sources and execute the compatibility gate
      shell: bash
      run: python "$GITHUB_ACTION_PATH/run_gate.py"
